# 采析绘（数据的采集、分析与可视化）

一个基于 Flask + Selenium + LLM (Ollama / OpenRouter) + scikit-learn 的多平台数据采集、数据清洗、
情感分析、通用数据分析与可视化工作流系统，内置**断点续跑**与运行记录管理：任务中断后可从检查点继续，
已花费的采集与 LLM 成本不会重复支付。

## 功能特性

### 数据采集
- **多平台爬虫**：支持知乎、微博、小红书、微信公众号、哔哩哔哩、抖音、YouTube、X（推特）的数据采集
  （Instagram 已可保存 Cookie，采集实现待接入）
- **采集量只由你填的目标数决定**：后端不再有任何翻页数/滚动轮数/时间窗宽度的硬编码上限（原有的
  「微博每窗 5 页、超过 720 小时拒绝、B 站 40 页、抖音 12 轮、YouTube 20 轮」等全部删除）。
  一直采到目标数满、或站点自己说没有了（空页、结束标记、游标不再给新东西）、或撞登录墙为止；
  目标数留空就是把这份列表采完
- **视频平台字段**：哔哩哔哩每行给出 播放/点赞/投币/收藏/转发/弹幕/评论数、时长、分P数、合集、UP主、发布时间与视频简介，
  数字取自视频详情接口而非搜索卡的「140.5万」式近似标签；
  抖音给出 点赞/评论/收藏/转发、作者粉丝与获赞、发布时间与完整文案（含话题标签），
  且**刻意没有播放数列**——抖音网页播放器不展示播放量，实测详情区的第二个数字就是点赞数，
  把它当成播放量写进表里是一个「列名看起来对、数字是错的」假数据，宁可不给
- **抖音必须可见窗口**：无头浏览器每次导航都会被挡在「验证码中间页」，因此该平台在类上声明
  ``never_headless``，执行器会在建浏览器之前自动改用可见窗口并在控制台说明，
  而不是默默给你一张 0 行的表。
  **入口 2026-09 重测后已改为直走路由**：搜索框仍然收得进文字、搜索按钮仍然可点，但**点击与回车
  都不再跳转**（地址一直停在 `/jingxuan`，用户盯着可见窗口看到的正是这个），而过去被记作
  "三个空 `<ul>` 空壳"的 `/search/<关键词>?type=video` 现在会给出结果列表——于是关键词直接写进
  路径，"要搜的"与"真搜的"不可能再不一致。卡片也从 `div.discover-video-card-item[data-aweme-id]`
  （现已匹配 0 个节点）换成 `[data-e2e="scroll-list"] a[href*="/video/"]`，视频 id 取自链接本身；
  列表**先挂 16 个骨架行**（无链接、无文字）、约 4~6 秒后才填真数据，所以等的是"有锚点"而
  不是"有节点"，永不填充才按 0 条处理；窗口滚动确实翻页（实测 16→26→36→46→56），
  达到目标行数后不再多滚一次
- **抖音「某作者的作品」**：填该作者的主页链接（`douyin.com/user/…`）或链接里那串 `sec_uid`——
  抖音 web 只认这个不透明 token（`/user/<数字>` 根本不是路由），而每个视频页都带着
  `a[href*="/user/"]`，所以这串号码是用户真能拿到的东西；填昵称会被直接拒收。
  **作品网格只认它自己的滚动容器**：实测把窗口滚到底只会喂页脚的推荐视频（8→29 条），
  网格一条不动——按"屏上锚点数"判进度就会在 57 条处收工，而那一页自报 85 条；
  改成把网格的可滚动祖先 `scrollTop = scrollHeight`，每轮稳定多出一批 18 条
  （实测 21→39→57→85，正好等于页面自报的作品数）。计数仍然逐条打开视频页取
  （列表卡片上那一个裸数字实测等于点赞数，所以这一模式也**没有播放数列**），
  因此作者模式与搜索模式的行字段完全一致。页面自报 0 作品→合法空表；
  自报 N 条却一条都没渲染→报可执行错误并引用页面自己的话，绝不伪装成"这个人没发过作品"
- **为什么默认不加载图片，却仍然加载视频**：图片对抓取没有任何用处（我们读的是文字与属性），
  所以浏览器层面默认拦掉，这是每次导航最实在的一笔节省。**但"人眼要看的窗口"必须显式允许图片**：
  抓取写下去的这条封锁会被 Chrome 存进持久 profile（实测一次微博抓取之后，
  `data/chrome_profile/weibo/Default/Preferences` 里留着 `images: 2`），而它跨会话存活——
  登录窗口只是"不写这个偏好"就会继承封锁，二维码是 `<img>`，于是页面开得出、码扫不了
  （用户实测：Cookie 更新不了）。现在取 Cookie 的登录窗与验证 Cookie 的探测窗都显式写"允许"。**视频不是不想拦，是拦了不省时间**——
  七个可爬平台逐一 A/B 实测（放行媒体 vs `Network.setBlockedURLs` 拦掉，拦确实咬得住：播放器
  `readyState` 从 4 掉到 0）：知乎 2.7s→2.5s、微博 2.0s→2.0s、哔哩哔哩 3.3s→3.5s、
  YouTube 9.5s→11.9s、X 19.3s→20.5s、小红书 20.6s→20.7s、**抖音 22.7s→26.5s**，
  也就是持平或普遍略慢。原因很简单：抓取**从不等待播放器**（页面策略是 `eager`，只读 DOM/JSON
  与接口），视频是在后台并行下载的，掐掉它并不会让我们更早拿到一行数据；而六个平台的抓取路径
  本来就**一点媒体都不下**（页内资源统计里媒体字节为 0），唯一会碰播放器的抖音逐视频详情页，
  整页 25.6 MB 里视频只占约 2%，大头是站点自己的 JS（`player-4.js`、`client-entry` 等），
  那些既不能拦也不该拦。**唯一的真实收益是省流量**：抖音每行约 0.6 MB，其余平台为 0。
  如果你按流量计费、愿意为它牺牲一点时间，可以把抖音这一家单独打开拦截（其余平台没有可省的），
  但默认不开——多一行数据要更久才到手，对"采集"这件事是净亏
- **评论采集**：数据源节点把「采集内容」切到**评论**，粘贴所选平台的文章链接（一行一个）即可批量抓评论；
  知乎/微博/小红书/哔哩哔哩/抖音/YouTube/X（推特） 各自只认本平台链接——面板样例、画布预检、引擎校验、抓取时过滤四层一致拒收外站链接，
  全部不符则报「与所选平台不符」可执行错误；
  哔哩哔哩评论还带 楼层/父评论ID/子回复数/用户等级/UP主态（子回复随父评论一并展开，未展开的余量以「子回复数」留痕）；
  抖音评论带 评论时间/评论地区（IP 归属地）/点赞数/子回复数，靠滚动评论容器翻页（其接口带 a_bogus 签名，无法直接请求）；
  可选每篇一个文件或合并输出；无评论/被登录墙拦截/链接失效三种状态分开上报，绝不静默吞成"空结果"
- **YouTube（已接入数据源）**：整站在页内走它自己的 innertube JSON 接口（打开 1 个页面拿配置，之后一轮 0.3~0.7 秒），
  因此**无头与可见窗口结果一致**、也不需要签名；「关键词搜索」每轮约 10 条、「某作者的作品」直接读频道 视频 页文档
  （每轮 30 条）、「评论」每轮 20 条并按**列表自己那条**游标翻页。行含 标题/正文(简介)/作者/频道ID/链接/播放数/
  点赞数/发布时间/时长/时长秒/分类/图片链接；「逐条获取精确数据」开关关闭时退回列表粗值以省时间，
  开启时每条多一次 11 KB 请求换回精确 播放数/点赞数/ISO 发布时间/完整简介/分类。
  抖音/YouTube 之外无 评论数 列的平台一律不编造该数（player 接口确实不给，就宁可不写这一列）
  （独立 Comment 节点仍保留，兼容旧画布，引擎共用，支持全部七个平台链接混合粘贴）
- **X（推特，已接入数据源）**：**只能可见窗口**——无头浏览器在 `/search` 上只会拿到注册浮层、
  在个人主页上直接吃 403，因此类上声明 `never_headless`（与抖音同为「模式无关、窗口类型有关」的拒绝）。
  时间线是**虚拟列表**：屏幕上恒为 13~21 个 `<article>` 节点，而实测八次滚动里流过了 139 条不同推文，
  所以进度不看节点数、只看**保留行数**，翻页信号是「屏上推文 id 集合变了没有」，续跑锚点是 id 集合而非列表下标；
  一张卡片只发一次 `execute_script`（旧写法每张卡十几次 `find_element`，50 行要多花约一分钟）；
  五个计数来自 `[role="group"]` 那一句 aria-label（"1887 replies, 7746 reposts, …, 1.1M views"），
  交给共享的 万/K/M 解析器，那句只写它有的数（"1 view"），按钮标签是退路，而 浏览数 只有这句里有；
  发布时间只存 `time[datetime]` 的 ISO UTC（屏上的 "13s/15h" 会在一次抓取中途变，存进表里就是每行一个不同的时刻）；
  搜索首屏实测 9.9 秒仍为 0 张卡、11.9 秒出 11 张，故首屏等待是 22 秒的**轮询**而非固定 sleep——
  8 秒就把这张表判成「这个关键词什么都没搜到」是假数据级的错误。
  三种模式：关键词（`f=live` 实时流）、某作者的作品（填 @handle 或主页链接）、评论（推文自己的永久链接页，
  同一条虚拟列表走法，根推文自己不会被当成回复写进结果）
- **知乎「某作者的作品」**：数据源节点的采集内容选**某作者的作品**，填作者主页链接（或 `/people/`
  后面那段 id），就依次采该作者的**回答**与**文章**两个标签页（实测 39 条起、滚动到 79 条，
  与搜索页同一种卡片，所以读卡逻辑完全复用）。两个实测决定了它的形状：
  ① **回答页里没有任何指向作者主页的链接**（86 个锚点 0 个 /people/），所以昵称没法用来找人，
  面板要求粘链接而不是填显示名，粘错成问题/回答链接会被明确拒绝而不是去开一个 404 主页；
  ② 列表项正文默认被「阅读全文」折起来，**就地展开**即可拿到全文（按钮变成「收起」，不需要跳转），
  而知乎的 `dateCreated` 元数据实测等于**最后编辑**时刻，所以发布时间取的是页面上的
  「发布于/编辑于」原文——前缀就是含义本身，去掉它等于把编辑时间写成发布时间
- **知乎搜索的正文是全文，不是摘要**：数据源节点多一个**展开全文**勾选（默认开）。这不是排版问题而是
  数据问题——实测搜索结果页的卡片里 `innerText` 与 `textContent` 相等、只有 35~109 字，且那节点
  `display:inline`、`overflow:visible`，**没有任何 CSS 折叠可破**：列表本身就只给了摘要，不点开的行
  存下来就是残缺正文（同一回答自己的页面是 308 字）。点开的方式是卡片自带的
  `button.ContentItem-more`（「阅读全文」，无 href），实测就地重画同一个节点（74→308、71→2103），
  URL 不变、21 张卡仍在、两行之后的句柄仍有效，8/8 次在 0.6 秒内落定。**只有链接里带 `/answer/` 的卡会被点**：
  专栏文章卡的同名按钮是真的跳转，会把后面所有卡片句柄全断掉（这就是本爬虫原本"绝不点卡片"的由来，
  而它把不该丢的正文一起保住了）；锚点还没挂上的卡种类未知，同样不点。
  展开之后**只替换 正文 这一列**：同一次点击会把日期标签从 `.SearchItem-time` 挪到 `.ContentItem-time`
  （`08-27` → `编辑于2026-08-27 12:22`）、也会把预览开头的 `作者名：` 前缀丢掉，若整行重读，
  发布时间这一列就会按"内部重试是否成功"混出两种格式、作者列会在最该有数据的行上变空。
  三行控制台话各说一件事：连续 3 次点开没反应即本次不再尝试（`连续 {n} 次展开均无回应…`，否则最坏是
  每行白等 4 秒）、本次一行都没展开时说明所有正文都是摘要（关掉勾选是用户的选择，但要留痕）、
  开着勾选仍有行没能展开时给出条数。每行代价一次 settle 等待（实测 < 0.6 秒，上限 4 秒）
- **哔哩哔哩「某作者的作品」**：填 UP 主空间链接（`space.bilibili.com/<UID>`）或那串数字 UID，
  读的就是这个页面自己渲染出来的投稿列表（一次导航 + 滚动翻页，实测一屏 40 条、滚三轮 80 条），
  每一行的数字仍走无需签名的 `x/web-interface/view`，所以**同一 UP 的一条视频在搜索模式和作者模式里
  字段完全一致**。它不调用 `x/space/wbi/arc/search`：实测那个接口对我们会话回 `code=-403 访问权限不足`
  （旧的无 wbi 路径还会撞 `-799`），伪造逐请求签名是另一个量级的问题，而列表本来就在页面上。
  UID 解析只认数字，粘成视频链接或 UP 主昵称会被明确拒绝——去开一个拼错的空间，
  回来的"这个 UP 没投过稿"就是假结论
- **哔哩哔哩「热榜」**：采集内容选**热榜**，再选榜单——**热门榜**是分页信息流
  （实测每页 20 条、第 2 页是新的一批），**周排行榜**一次就回整张表（实测 100 条）。
  这两个接口对我们会话回 `code=0` 且**每条自带 `owner`/`stat`/`pubdate`**，
  所以本模式**不需要逐条请求**：12 行只要 1~2 次接口调用，而关键词搜索因为卡片上只有
  四舍五入的"万"标签才必须逐条打开 `view`。行仍走同一个展平器，所以热榜的一行
  与搜索/作者模式的一行字段完全相同（不会出现"同一个视频两张表"）。
  采集次数是**可测的**：`Crawler.requests` 记录每一次导航与页内请求，
  真站层就是拿它断言"热榜绝不逐条取详情、整个榜单最多两次请求"
- **分批输出（不必等运行结束）**：采集节点可设「分批大小」，每满 N 行先落一个带编号的分片文件，
  运行中即可打开查看，节点结束时合并为单个文件（合并后是否保留分片可设置）；LLM 处理节点可勾选
  「实时导出」，每处理完一批就原子重写一份 `{节点}.live` 快照，随时打开都是当前完整结果
- **输出文件名可自动带上本次运行时间**：输出节点勾选「文件名追加本次运行时间（不覆盖旧文件）」后，
  落盘名变成 `导出名-YYYYMMDD-HHMMSS.csv`，同一秒里撞名再退让为 `-2`、`-3`；天天要跑的工作流
  不必再为每次运行手改文件名。时间戳只在**写文件那一刻**决定，绝不写进节点参数——参数一动节点指纹就变，
  续跑认不出自己、LLM 缓存整批落空、上游那一遍白爬。有一句实话要说清楚：**续跑时输出节点若被判为
  「可复原」就不会再执行，也就不会有第二个文件**；想再要一份请重新运行，而不是点继续。
- **同平台采集有两种不同的秩序（设置里两个键，别混为一谈）**：
  - **真排队**（`same_platform_queue`，默认开）：同一平台**一次只跑一条**，后一条要等前一条**整个跑完**
    才开始。这跟持久 profile 的承诺是同一件事（一个目录只开一个浏览器），所以开着它时并行同平台
    **不再弹「用不用 Profile」确认框**——这个决定已经替整台程序做掉了，问一次就等于允许那一次反悔。
    不同平台照常并行，串行画布一秒都不多等。**排队不给平台加冷却**是量出来的：微博当天两次
    「排队跑完不重叠、第二条仍被弹登录页」看着就像欠一次冷却，但随后同一账号 14 次采集——包括前一个
    浏览器关掉几秒后就开下一个——全部正常返回，所以那堵墙是站点间歇风控，不是发车太近（见
    `docs/crawler_notes.md`）。撞墙的真实处理只有一条：第 0 行就撞 → 等 `WALL_RETRY_BACKOFF` 重试。
  - **错峰**（把真排队关掉）：同平台两条采集**可以同时在跑**，只把它们的**发车时刻**按
    `same_platform_stagger` 秒隔开（默认 12，可 0~600，填 0 就是完全不隔）。隔开只发生在
    **确实同时在跑**的两条之间——串行一条跑完才轮到下一条，本来就碰不上，一秒都不等。
    这保住了真并行，也仍然
    避开站点最狠的形态——同一账号一秒内两次搜索（微博弹登录页、知乎回 40362）——但它管不了 profile
    冲突，所以那一次的 Profile 确认框会回来。
  - **例外：微博永远排队，关掉真排队也不例外**。矩阵把微博标为 `serial_only`：同一账号并行翻页
    （不只是同秒两次搜索，而是两条采集各自往 `page=2/3…` 深翻）必定被弹回登录页，这是实测的站点
    规矩，不是能被设置改掉的偏好。所以并行画布里有两条以上微博采集时，执行前会**单独弹一个窗**说明
    微博只能串行、继续即自动排队；错峰对它无效，其它平台照常错峰并行。控制台在真被排队时也播一句原因。
  间隔秒数交给用户，因为耐受度是账号的属性：12 秒是这台机器上把两类实测冲突都清掉的值，更忙的账号
  可能要 30~60，而从不同一平台跑两条时填多少都不掏时间。排过队/被错峰挡住的那条都会在控制台说明。
  另一处配套：**第 0 行就被弹到登录页**的采集会退避后重试一次（这是并发互撞的形状）；**已经采到行之后**
  才撞墙是 Cookie 中途死亡，仍走「判失败 → 断点续跑」，绝不重试，以免重爬已付费的行。
- **网络分区：国内与海外不能一起爬（设置项「国内与海外混采时提醒」，默认开）**。实测：挂 VPN 时抖音
  直接回 502 拒绝抓取，不挂时 x.com 根本打不开 —— 于是一张同时包含两边的画布，从任何一条网络跑都
  必定缺掉一半。按下执行时会先问一句（列出两边分别是哪些平台），**这是建议不是禁止**：真做了分流路由
  的机器两边都通，而这个页面分不出那种机器和"只是开着 VPN"。判定来自采集矩阵每个平台的 `region`
  字段（`cn` / `overseas`），前端与测试层都不另存一份清单；矩阵没说的平台不猜、也不打扰。
  同一套分区也切开了真机测试层：`live_cn`（关 VPN 跑）与 `live_os`（开 VPN 跑），见「测试」一节。
- **每平台一个持久浏览器 Profile（默认开启）**：抓取与"取 Cookie"用的是**同一个**浏览器目录
  （`data/chrome_profile/<平台>`），所以对站点来说我们是**同一台连续的设备**——这直接针对
  实测到的两类失败：微博在任何一次已登录页面加载后就会**换票**（`SUB` 90→94 字节、
  `SUBP` 144→56），文件里那份旧的第二次重放就不被接受；小红书会在十几分钟内把
  我们"重放快照"的搜索会话踢回登录页，而用户自己的浏览器一直正常。
  规则是：**目录被用过之后就不再往里种 Cookie**（只有首次导入 `data/cookies/*.json`）——
  把旧快照盖回一个活着会话的 profile，正是我们刚实测到的那种自伤，
  所以抓取结束也**绝不**把浏览器当前的 Cookie 回写文件（回写实测会把可用会话写坏）。
  「设置」面板里能看到每个平台 Profile 的现状（未建立 / 首次将导入 / 已在使用），
  并标出哪些平台**建议开启**；关闭该功能时，含这类平台的工作流会在运行前弹窗提醒，
  选「先去设置」会直接打开面板。也可以填一个绝对路径，改用你专为该工具新建的 profile
  目录（**不要**填你日常使用的 Chrome 目录：Chrome 运行时独占该目录与其 Cookie 库，
  且默认 profile 禁止远程调试，实测拿不到也带不进去）。
- **并行 + 同平台 = 运行前问你一次**：一份 profile 同时只能开一个浏览器（chromedriver 建会话前
  要预写该目录的偏好文件，实测两个线程同时建会话 8 次里死 2 次，报 `session not created:
  failed to write prefs file`）。所以画布里有**两个以上互不相连的工作流采集同一平台**、且当前是并行模式时，
  执行前弹窗让你本次二选一：
  ① **继续用 Profile**——站点看到的是同一台连续设备（微博/小红书就是针对一次性浏览器的），
  但这些采集会一个跑完再跑下一个（控制台会说排队等了多久），这部分并行等于没有；
  ② **本次不用 Profile**——它们真的同时爬，代价是每个都是全新设备 + 导入一份旧 Cookie 快照，
  正是那些站点会拒绝的形态。
  答案只作用于这一次执行（写进这次运行的请求体，不改设置）；取消即不开始。
  同平台但**在同一个工作流里**、或不同平台、或串行模式，都不会问你（没有可选项的问题就是噪声）。
- **Cookie 管理**：持久化登录状态，避免重复扫码（知乎/微博/小红书/哔哩哔哩/抖音，加上 **X（推特）/
  Instagram / YouTube** 共八个平台；微信**不在其中**——抓推文正文本来就不需要登录，唯一需要后台登录的
  按关键词搜索因实测不可验证已移除）。
  其中 **Instagram 目前只能保存登录 Cookie，还不能作为数据源采集**：画布的数据源节点不会
  列出它，即便手写进工作流也会被按节点名明确拒绝，而不是回一张空表（YouTube、X（推特）已实装，见上）。
  Instagram 卡在实测而不是卡在工时：带有效会话的可见窗口、无头、退出登录三种情况下，主页都只渲染
  头部信息（昵称、1.04 亿粉丝、简介）与**零个**作品卡片，话题页直接跳 `/accounts/login/`；
  在没有观察到真实作品列表之前，照着一份不存在的 DOM 写抓取就是猜。
  扫码生成为后台单飞任务——登录期间设置面板保持打开，
  提供「已完成登录 / 取消登录」按钮，窗口被关闭会自动判错回收；全过程进控制台日志。
  面板现在按平台**说明用途与获取步骤**（由 `/api/cookies/flow` 提供，中英双语），并可粘贴
  「登录入口链接」——只接受该平台域名，越界链接会明确告知改用平台登录页，绝不把别站的 Cookie
  存进本平台文件：取 Cookie 前浏览器会先被带回本平台页面，粘贴进来的 JSON 也会按域名过滤
  （Google/Facebook 这类登录中转站的 Cookie 一律不收，`evilyoutube.com` 这种伪后缀同样拒）。
  另有**「验证 Cookie」**：带已存 Cookie 实地访问一次，把结论逐条回显到面板，
  而不是让用户靠"抓出来 0 行"去猜 Cookie 是不是废了。结论是**三种**而不是两种：
  被弹回登录页 = 失效；页面正常 = 可用；回验证码/风控 = **没能验证**（这既不算可用也不算失效，
  拿它当"失效"会让人白重登一次会话）。
- **运行前自动验证 Cookie（设置里可关）**：按下执行时，先用**这一次运行真正会用的浏览器**
  逐平台加载一次该平台首页，问它还不认这份 Cookie。哪个平台回登录页，运行就地被拦下——
  弹窗点名平台、引用后台给出的原话，并直接把你带到该平台的 Cookie 面板；这里**刻意不留**
  「我确定，照样跑」，因为从登录页开始的抓取一小时后只会带回一张空表。
  核不上的平台（验证码、超时、profile 正被一场真抓取占用）只说明「无法核对」，**不拦**：
  没有答案不等于 Cookie 死了。结论缓存几分钟（`COOKIE_PREFLIGHT_TTL`），同一画布连着按两次只付一次浏览器；
  保存 / 删除 / 扫码取回 Cookie 都会立刻作废缓存，免得拿着旧结论拒绝刚修好的运行。
  关掉这一项 = 什么都不问、什么都不拦，直接开始运行（旧的「执行前确认 Cookie」弹框已删除）。
- **「删除已存 Cookie」**：Cookie 面板可按平台删除那份 JSON 快照。删除框与删除后的回话都会说清
  一个容易搞反的事实：抓取若跑在该平台自己的 profile 里，登录态存在 profile 里，
  **删文件不会把那个 profile 登出**（profile 只在第一次使用时导入快照，之后不再被覆盖），
  它清掉的只是"一次性浏览器"要种的那份快照。要真正换设备得重置 profile 目录。
- **微信的特殊处理（实测后确定）**：微信只采集**文章正文相关字段**，不采集**留言、点赞数、转发数**
  （多数会话里连阅读数也没有）。原因：微信把这几项放在只下发给微信客户端会话的凭证之后——真实浏览器里
  实测页面 `show_comment=0`、3.4 MB 的文章 HTML 中 `elected_comment` 出现 0 次、
  直接调 `mp/appmsg_comment` 返回一段验证页「请在微信客户端打开链接」；换 `MicroMessenger`/`XWEB` UA、
  改写链接参数、甚至重放微信客户端自己 Cookie 库里的 `pass_ticket`/`appmsg_token` 都不改变结果。
  本项目**不伪装微信客户端**绕过该判定；更关键的是，浏览器里"这篇文章没有评论"与
  "这次访问不被允许查看"完全无法区分，返回一张空表等于交出看着正常的假数据。
  因此数据源节点选中微信时不提供「采集内容=评论」，并在面板上给出可点开的**原因说明弹窗**
  （`explainWechatLimits()`）。评论采集请使用 知乎 / 微博 / 小红书。
  批量抓取在控制台**每篇只留一行**（成功：第 i/n 篇 + 标题 + 公众号；失败：第 i/n 篇没取到正文），
  逐字段/滚动/预览那段步骤轨迹降到 debug 只进 `logs/app.log`——以前一篇刷十行、还把正文前 120 字
  贴进共享控制台，用户真正要问的"这篇到底成没成"反而找不到；微信的任何一条消息也不得承诺
  阅读/点赞/打赏这三个它取不到的数字
- **微博"假登录墙"免疫**：微博搜索会先把浏览器闪到扫码页再弹回已登录结果流（站点设置的假象）；
  爬虫会等待页面进入终态（渲染出卡片 / 明确无结果 / 持续停留在登录页）后才判定登录墙，
  真掉线时照常明确报错，已采数据不受影响
- **运行时浏览器设置**：chromedriver 路径、浏览器二进制、窗口尺寸、超时等可在设置面板修改，
  写入 `data/settings.json`，下次运行即生效，无需改代码重启

### AI 分析
- **双 AI 提供方**：本地 Ollama 或 OpenRouter API（免费模型列表一键拉取、连接测试），
  模型选择、批量保存行数、截断长度均可在 AI 面板配置；OpenRouter Key 仅存浏览器 localStorage，不落服务器
- **情感分析（双模式）**：LLM 逐行深度分析，或传统 ML（sklearn TF-IDF + 逻辑回归）批量高速推理，随时切换
- **倾向性分析（双模式）**：同上，LLM 或 ML 可选
- **语义数据清洗**：自动过滤广告、无关内容与低质量数据（LLM 判定）
- **关键词提取**：TF-IDF / TextRank（依赖 jieba）
- **文本聚类**：K-Means / DBSCAN 自动发现文本分组
- **命名实体识别（双模式）**：正则规则（人名/机构/地名/日期，零依赖），或改用大模型抽取；可只保留四类中的某几类
- **异常检测**：Isolation Forest 自动标记数值异常行（少于 5 行、或没有可用数值列时**明确报错**，
  不会给每行填 0 分冒充"已检查、无异常"）
- **相关性分析**：Pearson / Spearman / Kendall 相关系数矩阵（点名的列全是文本时报错并点名该列；
  混选时只算可用列并在控制台说明跳过了哪些）
- **模型训练**：从已有 LLM 标注数据一键训练传统 ML 模型，后续推理无需 Ollama

### 数据处理
- **通用数据分析节点**：去空值、去重、条件筛选、重命名列、类型转换、排序、采样、分组聚合、
  表关联、列计算、数值分箱等确定性清洗，可独立于爬虫直接处理任意表格数据；
  这些算子的细节参数现在都能从面板设置：**去空值**可选「任一列为空即删」或「全部列为空才删」，
  **填空值**可选用给定值填充或前向/后向填充（ffill/bfill），
  **数值分箱**可填等宽箱数（如 `4`）或直接给边界（如 `0, 60, 80, 100`）并配箱标签；
  算子被拒（写了不存在的算子、给文本列求和、比较条件填非数字）时**节点判失败并写明原因**，
  不再回一张空表让运行看起来成功、导出只剩表头的文件
  ——此前这三处参数在面板与后端之间被丢弃，选了也不生效
- **分词节点**：jieba 对任意文本列分词，输出词频对，供导出或词云使用
- **文件持久化**：上传/粘贴的文件注册为数据集（内容哈希寻址、zlib 压缩存库），
  保存的工作流随时可重新加载自己的输入文件；无引用且长期未用的文件自动清理
- **通用导出节点**：一个节点支持导出 CSV / JSON / Excel / TXT / HTML / Markdown 六种格式
- **一键运行报告**：工具栏「生成报告」把本次运行（或运行记录里任选一条历史运行）的每张表格与服务端
  渲染的图表拼成**一个自包含 HTML**——样式与图片全部内联，离线能看、能打印、能发给别人；
  文件仍落在导出目录，由「导出产物」面板统一查看与删除。可选「附 AI 结论」，只根据统计数字让模型写一段话，
  结论失败仅少一段文字而不影响报告本身。因为内容来自互联网抓取的文本，报告页只经 `/api/report/view`
  提供，并带禁止脚本的 CSP 策略

### 可视化
- **通用可视化节点**：柱状图 / 折线图 / 饼图 / 散点图 / 直方图 / 箱线图 / 热力图 / 桑基图 /
  词云（支持中文分词）/ 中国地图，支持 ECharts（前端渲染）与 Matplotlib（服务端渲染）两种引擎
- **采集深度可调**：微信正文长度上限由 `Config.WECHAT_BODY_MAX_CHARS` 决定（默认 5000，
  填 0 完整保留；被截断时行尾带 `…` 标记，免得"半篇"看起来像"写完的短文"）；
  小红书数据源节点新增**每篇评论预览数**，填 `0` 真的跳过评论面板（以前即便只要 0 条，
  每篇仍会等满 10 秒，最快的设置反而是最慢的）
- **Chart Studio 图表工作台**：画布内嵌 ZENVIZ 工作台，可对任意节点/数据集的完整表格做自由图表创作，
  支持多数据源合并，成品 PNG 可保存回导出目录
- **数据预览面板**：任意数据集都能以可翻页的表格形式查看真实数据行
- **仪表盘看板**：把画布上所有 Visualize 节点的图表拼在一个网格里一起查看
- **实时统计**：ECharts 图表展示情感/倾向性分布与执行汇总

### 工作流引擎
- **工作流画布**：拖拽式节点编辑，可视化配置采集/清洗/分析/可视化全流程
- **版面约束**：窗口与页面级**绝不出现水平滚动条**；面板内的表格**可以**横向滚动，但被截断的
  单元格必须带省略号（静默裁掉等于让用户读不到数据），长关键词不再把节点撑到视口外；
  中英文两种语言都在真 Chrome 里量过一遍（英文标签更长）
- **底部三面板互斥**：控制台、运行记录、导出产物共用同一个底部槽，打开任一个都会收起另外两个
  并清掉它们残留的内联高度（只去掉 `.open` 时，拖拽写入的内联高度会盖住 CSS 的 `height:0`，
  于是两个面板叠在一起）；三个面板都可拖高
- **控制台的两条读法约定**：
  ① **切换工作流标签页不再清空日志**——服务端只保留整次运行的最后 200 行，切走的标签页无法再
  向上一次请求"要回"自己看过的内容，所以每个标签页（含「全部」）在浏览器里各自留一份已显示历史，
  切回来时按自己的历史重画；每个标签页的游标也各自前进，因此后台标签页的行不会因为"没在看"而丢，
  切回来也不会重复播一遍，最多各留 500 行。② **「工作流命名」「上传文件」节点不再播报执行信息**——
  前者只带一个名字、后者只重读用户早指过的文件，为它们印"正在执行节点…"+"节点…完成(2/4)"是纯噪声；
  被压掉的只有这两句套话，上传节点自己那句"已载入 xxx.csv：N 行"、以及任何失败/跳过/复原的原因照旧送出
- **界面四处修正**：① Cookie 面板那行"哪个平台存了 Cookie"以前固定印 `zhihu: OK`，中英都不跟、
  `OK` 还替它答了一件它不知道的事（有文件 ≠ 会话还有效），现在逐平台用界面语言说「已存 Cookie / 未存」；
  ② 凡是"要丢东西"的确认（删除运行记录、重新开始、删除导出文件）不再用浏览器原生 `confirm`，改回页面
  自己的对话框——原生的既不翻译也不能样式，还只回一个 true/false，说不清要删什么；③ 断点续跑横幅在菜单栏
  钉住显示时**向下让开整条菜单的高度**（带 180ms 过渡），以前它固定 `top: 8px`，正好被那条固定定位的
  菜单盖住，而那上面就是「继续」按钮；④「导出产物」的"共 N 个文件，合计 X"改成**表格下方的居中总结行**
  （它说的是整个导出目录，不是屏上某一行；放在表格上面会被读成第一条文件的数字）。
  另外所有"平台名"位置都统一了：`zhihu` 是矩阵与 Cookie 文件的**键**，不是给用户看的词——控制台的
  `{platform}` 现在由后端翻译（列表按 `,`/`、` 分隔才逐项翻译，`/` 也分会把 `../x` 这类被拒绝的值改坏），
  前端弹窗同一份词表，两处由 `TestPlatformLabelParity` 钉住相等。
- **工作流命名**：保存时可命名（画布 Name 节点即工作流名），历史与运行记录按名称归组
- **运行队列**：一次只跑一个运行是这套设计的前提（控制台、浏览器、断点游标都只有一个写者），
  但"忙"不再等于"白按一次"——运行中再按「执行」，按钮变成「排队运行」，请求被服务端存下，
  当前运行结束后自动接着跑；排队列表显示在「运行记录」面板顶部，可逐条取消（最多 8 个，满了直接拒绝而不是无限堆）。
  队列只在服务进程内存活（重启即清空），因为"没人确认的意图"不该假装被记住了
- **多线程执行**：支持并行/串行执行模式，可随时停止；进程管理面板可强杀残留浏览器进程。
  「停止」之后还要**等自己启动的节点线程真的退出**才交还运行槽：并行模式一条工作流一个线程，
  旧写法 `shutdown(wait=False)` 直接走人，于是被停掉那次运行的最后一个节点继续往
  下一次运行的控制台、节点计数与 results 里写——症状是下一次运行"播报了它没做的采集"
  （`/api/workflow/stop` 会关掉在跑的浏览器，所以被等住的最多是"一页加载"，不是一整次抓取）
  「停止」现在**不再拖住运行记录**：浏览器改到后台线程逐个关，停止请求立刻返回；撞墙退避那
  45 秒会随停止立即中断，运行线程很快走到收尾把记录判成「中断」。运行记录面板在运行中每秒
  跟着刷新，按停止后也会续读几条直到那行不再写着「运行中」——停止的 toast 只说"正在停止"，
  结论由运行自己写。
- **撤销/重做**：Ctrl+Z / Ctrl+Y 支持 50 步历史回退，菜单和右键菜单均有入口。
  历史快照必须**深拷贝节点参数**（以前直接引用活对象，而参数编辑是原地改，于是全App 任何
  参数改动都撤销不掉，且后续修改还会改写历史里的旧条目）；相同的画面不入栈
  （每 30 秒的自动保存以前会把 50 步真实历史挤成 50 份"现在"，闲逛半小时后 Ctrl+Z
  变成什么都不做）；打开一份草稿只算**一个**撤销点（以前每个恢复出来的节点各推一次，
  开页面先按 11 下撤销是在删你没加过的节点）
- **节点重命名**：双击节点标题（或右键→重命名）即可命名节点；控制台与校验信息以「名称 #节点号」
  播报、工作流前缀显示工作流名（不再是 WF0/WF1），一眼对上画布上哪个节点在说话；跳过的节点不再
  谎报"完成"；自定义名在保存/加载、撤销重做、切换语言后都保留，清空即恢复类型默认名
- **一次按键说完所有问题**：toast 只有一个元素、文字是替换的，旧写法遍历校验错误等于只让你看见
  **最后**那一条——五个问题就要按五次「执行」才逐个问出来。现在一条 toast 带计数与编号，读完为止
  （多行时按行数延长停留），并且换行在界面上真的成行、也不会把页面顶出水平滚动条；只有一个问题时
  仍是那句话说完就够，不套列表头
- **节点 id 不是页面 id**：节点框的 DOM id 就是它的 id，而工作流文件里的 id 原样被采用，所以
  按 id 去 `getElementById` 是在问"页面上有没有这个元素"——一个叫 `status-zoom` 的节点会让
  删除节点与撤销路径把底部状态条整个删掉。所有节点元素改由 `canvas._nodeEl(id)` 取（即
  `nodes[id].el`），并用文本门禁止 canvas.js 里再出现"变量参数的 getElementById"
  （DOM 桩表达不出同名 id 的冲突：桩让后写的赢，浏览器按文档顺序让先写的赢）
- **切换语言到达每一张 JS 生成的表**：运行记录、导出产物、数据集管理、执行历史四张表的表头与
  状态词都是 JS 从词典拼出来的，`I18n.apply()` 只重刷带 `data-i18n` 的静态元素、走不到它们——
  以前切到 English 后这两张表还是中文，要关掉面板再打开才换。现在面板开着就当场重画（执行历史
  用上一次的返回结果重画，不再多问服务器一次）
- **右键菜单不说假话**：没复制过任何东西时不再列出「粘贴节点」（它的动作本身是静默拒绝的，
  点下去没反应只会让人以为菜单坏了）；折叠过的节点那一行改说「展开」并把动作换成
  `ctxUnfoldNode`；菜单位置按它自己实测的宽高钳位，不再用 200px 魔数
- **节点折叠**：选中节点按 F 键或右键折叠，节省画布空间
- **自动布局**：按 DAG 层级 BFS 自动排列所有节点
- **节点颜色编码**：节点库按 工作流 / 数据输入 / 数据处理 / 结果输出 四组排列，各节点有独立颜色与标识
  （NAM/SRC/CMT/UPL/RSM/PRC/ANL/TKN/VIZ/OUT）

### 断点续跑与运行记录
- **节点级检查点**：每步输出行持久化到 `data/runs.db`，工作流结构指纹 + 节点指纹决定哪些结果
  可复用、哪些因参数改动而失效重算。**标签不算参数**：改「工作流命名」的名字、或把上传文件名
  改得更好看，都不会让已付过费的行作废（名字是记录怎么写，不是爬虫读什么）；数据集/运行的 **id**
  仍在指纹里，指向另一个文件就必须重算
- **LLM 答案缓存**：按 操作+提供方+模型+列+截断+主机+主题+提示词版本 缓存回答，重跑或续跑不再为
  同一行付费；改了提示词模板或参数，旧答案自动失效，绝不"新提示词吃旧结果"
- **采集去重指纹（增量采集）**：已抓到的条目指纹按节点入库，续跑/重跑自动跳过已采集内容并在控制台
  明示跳过数；采集节点可勾选「重新采集」清除账本全量重抓
- **中断自动识别**：服务重启后遗留的"运行中"记录自动标记为"已中断"，画布顶部出现续跑横幅，
  一键「继续」或「重新开始」（前提是本机同时只跑一个服务——见「启动应用」一节，
  两个实例会互相把对方正在写的运行判成中断）。判定时**按已落库的行回填「已存行数」**：
  进程被硬杀时收尾那一步根本不会执行，行数其实一直安全地存在 `node_rows` 里，若只改状态不回读，
  记录就会说"保住了 0 行"——而 Resume 节点正是按这个数字挑"最满的节点"来收养，等于把付过费的
  数据当成空的丢掉
- **续跑可以连着点**：上一次只做「复原」的节点（状态是 restored 而不是 done）在下一次仍然算已
  完成、仍然直接回读，不需要重新计算——否则"不重复付费"这件事只兑现一次点击，第二次「继续」就把
  上一轮刚复原的节点全部重跑重付
- **完成数只有一个口径**：运行记录里的 `n/N` 与控制台的「N 个节点完成」说同一个数——跳过、失败、
  部分完成的节点都不算完成（以前记录把失败也算进 n，同一条运行在两处显示成 1/2 与 0/2）；
  结束行里"几个失败"也只数**这次画布上真跑过的**节点，已删掉的节点不会在每次「继续」时
  重新失败一遍、把一条其实已跑完的运行永远留在续跑横幅里
- **Cookie 中途过期可续跑**：长采集常跑到一半 Cookie 失效被挡回登录墙——已抓的行早已落库，
  节点判为"部分完成"、整条运行标为失败进入续跑横幅；浏览器弹 toast + 控制台明示「更新 Cookie 后继续」，
  换新 Cookie 点「继续」即从断点接着抓、绝不重复已存条目；恰好在目标达成后才撞墙则视为完整，不误报。
  撞墙那一行只陈述被挡回登录页，不再替爬虫宣称"已停止本次抓取"——这句写在通用的登录墙判定里，
  而各家策略不同（知乎/微博/B 站确实停，微信批量是跳过这一篇继续下一篇），说停了就是假话；
  真停下时由节点用「登录态疑似失效」那句报，并给出该怎么做。**评论**节点同样只点名真正被撞回的那个
  平台（以前硬写四个平台名，爬 YouTube/X 被撞墙时报告里没有一个名字是对的）。
  爬虫自己抛错退出（知乎撞墙即抛）时也照样发布 Cookie 失效标记并合并分批文件——以前这两件事写在
  抛出之后的代码里，最容易撞墙的知乎反而什么都收不到，只有一行不带 Cookie 字样的错误
- **执行前 Cookie 确认（可关）**：含采集/评论节点的工作流运行前弹框提醒「Cookie 是否久未更新，
  先退出更新还是继续执行？」，退出即取消本次执行；默认开启，可在设置面板关闭（关闭后行为与从前一致直接执行）
- **Resume 节点**：直接收养历史运行的节点输出作为数据源——原始平台已登出/被封/已花过成本时使用
- **导出产物面板**：`data/exports` 从此可读——按时间倒序列出导出文件（名称/类型/大小/修改时间），
  支持逐个下载与逐个删除，并显示目录合计；文件名一律按名解析，
  `../`、绝对路径、子目录一律 404（与"文件不存在"同一应答，避免变成探测外部路径的探针），
  脚本/可执行一类扩展名（`.py`/`.js`/`.html`/`.exe`…）在列表里可见但**不下发**（可删除）
- **运行记录面板**：列出各工作流的运行历史、节点状态、行数统计，支持丢弃、删除与按保留策略清理。
  「继续」之后的那次尝试会**改写记录的描述字段**（mode/无头或窗口/工作流名/结构指纹/语言）——
  续跑时把并行改成串行、或把名字改了，标签必须说这次真实怎么跑的；`started_at` 保持原样，
  那是这份工作第一次开始的时间，而且它正是预览/图表/导出查回旧行用的键
- **并行 = 一条记录，写全部工作流的名字**：画布上几个工作流（各带自己的 Name 节点）同时开跑，就是
  一次运行、一条记录——名字按画布顺序用 ` + ` 连起来（如 `热门榜 + 周排行榜`），行名旁边再打标。
  **标签说的是"这次实际怎么跑的"**：`并行 ×N` 只在 mode 为 parallel 时出现，再加 `无头` 或 `窗口`。
  以前只写第一个名字，用户看到的就是"热门榜的记录把周排行榜吞了"。合名也是查回旧行的键，所以单个
  名字的旧记录照样能预览（两种拼写一起给）；重名的两个工作流只写一次名字，但 `×N` 仍说明真跑了 N 个
- **串行 = 每个"轮到执行的工作流"一条记录**：串行是一个一个轮着跑，所以每个**开始执行**的工作流
  写自己的一行（名字就是它自己的标签、`串行 ×1`、各自的状态与行数、各自可续跑），
  **中止时还没轮到的那个一条记录都不留**——它什么都没发生；已发生的那条保留自己已付的行与游标，
  下次从它自己的断点接着跑。两条实现细节是这条规则能成立的前提：① 每行存的仍是**整张画布**的结构
  指纹，因为续跑横幅与 `/api/runs/resumable` 就是按它匹配的，拆行不能把这个键拆开；② HTTP 响应
  交回的 `run_id` 是**第一个工作流那一行**的 id（浏览器拿它轮询状态、拿它点继续），不能让它指向一个
  不存在的行；③ 再点「继续」时各工作流按名字找回自己那一行**累加**，不是每按一次就多一对记录
- **一条记录里含多个工作流时，展开按工作流分组**（并行的那一条，以及拆分之前写下的旧记录）：
  每个节点存下它属于第几个工作流、以及那个工作流叫什么（`node_runs` 的 `component` 与
  `component_name`，旧库由 `_ensure_columns` 自动补列，老记录读作 0 与空 → 面板照旧不加标题）。
  分组标题的名字**与控制台用的是同一个函数**（`_wf_display_name`），所以不会出现"面板叫甲、控制台叫乙"；
  它随每次访问刷新，因为把节点从热门榜改接到周排行榜之后再点详情，还写着旧名字就是假陈述。
  每组显示"几个节点完成 / 共几行"，其中 `restored`（只回读了上次结果的节点）计入完成、`partial`
  与 `failed` 不计——与运行行上的 `n/N` 同一个口径
- **并行记录的续跑是每条工作流各自的状态**：一条工作流失败不会让另一条重跑（未改动的节点回读已存
  行、标为"已复原"），改动了 A 的参数也只重算 A；每条工作流取回的是自己的行，绝不把热门榜的表贴到
  周排行榜名下
- **执行历史与趋势对比**：自动记录情感/倾向性分布与各节点行数，可按工作流和指标画出时间序列；
  列表里**每一条运行都能单独删除**（只删那一次的指标，其余历史与趋势图保留），不必为了去掉一个
  跑坏的执行而「清空历史」把整张对比图一起丢掉
- **保留策略**：每工作流默认保留最近 20 次运行、30 天内的记录；干净完成的先清理，
  中断的最后清理（还可能被续跑）；单节点行数与单文件行数均有硬上限防止撑爆磁盘。
  策略**自动生效**：服务启动时清一次，此后每次运行结束按时间闸（默认 60 分钟）再清，
  不必记得手动点「清理」；清理只删真正过期/孤立的条目，正在写的运行记录永远豁免。
  **被自动裁剪掉的运行会同时交回它的"已采集"认领**：行已经不在了，留着认领就等于让以后同一次
  采集静默少给条目、且无从解释（手动删除某条运行仍保留认领——那时是用户自己知道这一趟已经爬过）
- **运行日志自动轮转**：`logs/app.log` 写满 5 MB 滚动为 `app.log.1 … app.log.5`，
  长时间抓取不会把日志撑到无法打开（上限与份数在 `config.py` 可调）

### 界面
- **图表库离线也不拖垮整页**：ECharts 从 CDN 载入，而这台机器可能没网。以前
  `stats.init()` 无保护地调 `echarts.init`，而整个启动过程只有一个
  `DOMContentLoaded` 主体——那一处抛错顺带带走了平台能力清单的拉取、上传文件的
  重新挂接、30 秒自动保存与断点续跑横幅，屏幕上的症状却是"数据源节点全说读不到能力"。
  现在每个启动步骤各自兜住异常（末尾提示有几个模块没起来，详情在浏览器控制台），
  图表区缺库时明说"图表库未加载"而不是留一个看起来像"没有数据"的空框
- **中英双语**：前后端消息全套 i18n 目录，界面一键切换语言，日志按请求语言输出
- **亮色主题**：设计系统化的浅色界面，网格背景画布，主题色/圆角可调

## 快速开始

### 1. 环境要求

- Python 3.11+（使用 `zip(..., strict=True)`）
- Chrome 浏览器（Selenium 驱动，驱动路径可在设置面板修改）
- [Ollama](https://ollama.ai/) 本地大模型，或 OpenRouter API Key（二选一即可）
- 如需 Matplotlib 引擎渲染中文图表，服务器需安装中文字体
  （如 `fonts-wqy-microhei` / SimHei / Microsoft YaHei 任一即可，
  Linux 下可 `apt install fonts-wqy-microhei`）

### 2. 创建虚拟环境并安装依赖

> 所有运行与安装一律在项目虚拟环境 `.venv/` 中进行，勿使用系统 Python。

```bash
cd crawler_workflow
python -m venv .venv
source .venv/Scripts/activate        # Windows Git Bash；cmd 下用 .venv\Scripts\activate
pip install -r requirements.txt
```

### 3. 下载 Ollama 模型（可选，用 OpenRouter 可跳过）

```bash
ollama pull qwen3.5:9b
```

### 4. 启动应用

```bash
cd backend
python app.py
```

访问 http://127.0.0.1:5000（端口通过环境变量 `PORT` 修改，监听地址通过 `HOST` 修改）。默认只监听本机、
且不自动开浏览器；开发时用 `FLASK_DEBUG=1` 打开调试模式（含自动打开浏览器与代码热重载）。

**注意：一次只能运行一个 `app.py` 服务。** 本项目是按"本机单人使用"设计的：所有状态都存在同一份
`data/` 里（`runs.db`、`datasets.db`、`cookies/`、`exports/`），并且服务启动时会自动把数据库里状态仍为
"运行中"的运行判为**已中断**（那是上一个进程留下的痕迹，也是续跑横幅的来源）。所以如果同时开着两个
实例（比如 5000 和 5057 各一个），后启动的那个会立刻把前一个正在写入的运行标成中断，两边还会互相
改写同一个节点的行与游标。要换端口就先停掉旧的实例，不要并行开着。

## 项目结构

```
crawler_workflow/
├── backend/
│   ├── app.py                    # Flask 主入口（路由 + 执行编排）
│   ├── config.py                 # 配置与数据路径
│   ├── i18n.py                   # zh/en 消息目录（X-Lang 请求头切换）
│   ├── settings_store.py         # 运行时可改设置（data/settings.json）
│   ├── crawl_capabilities.py     # 采集矩阵：平台 × 模式 × 字段 × handler（校验、执行、前端面板的唯一口径）
│   ├── crawlers/                 # 爬虫模块
│   │   ├── base.py               # 爬虫基类（登录墙判定、Cookie 播种、流式落盘）
│   │   ├── zhihu.py              # 知乎爬虫
│   │   ├── weibo.py              # 微博爬虫
│   │   ├── xiaohongshu.py        # 小红书爬虫
│   │   ├── wechat.py             # 微信公众号爬虫
│   │   ├── video.py              # 哔哩哔哩 / 抖音（搜索、某作者、详情、评论）
│   │   ├── youtube.py            # YouTube：页内 innertube JSON（搜索 / 某作者 / 评论）
│   │   ├── twitter.py            # X（推特）：可见窗口读虚拟时间线（搜索 / 某作者 / 回复）
│   │   ├── comments.py           # 评论采集适配器（七个平台的链接可混粘贴）
│   │   ├── overseas.py           # Instagram：只做 Cookie 捕获，尚无抓取
│   │   └── engine/               # 与任何站点无关的机械层——新抓取一律先走这里，不在平台模块里复制循环
│   │       ├── counters.py       # 一个解析器读遍 万/千/亿 与 K/M/B（「97 views」「1,027,710次观看」）
│   │       ├── wall.py           # 登录墙 / 风控页 / 弹回首页三种拦截的判定
│   │       ├── popup.py          # 首次进入的对话框（平台只声明提示词与按钮）
│   │       ├── feed.py           # 无限列表的滚动收割（含虚拟列表的「窗口换了没有」信号）
│   │       ├── pager.py          # 跟着服务端自己给的游标翻页，不自增页码
│   │       ├── jsonpath.py       # 在页面内嵌 JSON 里取值
│   │       └── innertube.py      # YouTube 的页内 JSON 传输与「这条分页属于哪个列表」选择
│   ├── analyzers/                # 分析模块（LLM + ML 双模式）
│   │   ├── ml_base.py            # 传统 ML 底座（TF-IDF + 分类器）
│   │   ├── llm_client.py         # Ollama / OpenRouter 客户端
│   │   ├── cleaner.py            # 广告/噪声语义清洗（LLM）
│   │   ├── emotion.py            # 情感分类（LLM + ML 可选）
│   │   ├── tendency.py           # 倾向性分析（LLM + ML 可选）
│   │   ├── keyword.py            # 关键词提取（TF-IDF / TextRank）
│   │   ├── clustering.py         # 文本聚类（K-Means / DBSCAN）
│   │   ├── ner.py                # 命名实体识别（正则规则 + LLM 双模式）
│   │   ├── anomaly.py            # 异常检测（Isolation Forest）
│   │   └── correlation.py        # 相关性分析（Pearson / Spearman / Kendall）
│   ├── engine/                   # 工作流引擎
│   │   ├── workflow.py           # DAG 解析、调度、节点校验与指纹
│   │   ├── executor.py           # 线程池执行器
│   │   └── logger.py             # 日志管理
│   ├── services/                 # 服务模块
│   │   ├── run_store.py          # 断点续跑核心：运行/节点/行/指纹/LLM缓存（runs.db）
│   │   ├── dataset_store.py      # 上传文件持久化与工作流引用（datasets.db）
│   │   ├── stats.py              # 情感/倾向性统计服务
│   │   ├── cookie_manager.py     # Cookie 管理
│   │   ├── cookie_flow.py        # 各平台 Cookie 的用途/获取步骤/入口链接白名单
│   │   ├── workflow_manager.py   # 工作流持久化
│   │   ├── exporter.py           # 通用多格式导出服务（Output 节点）
│   │   ├── export_browser.py     # 导出目录的读取侧：列表/下载/删除与路径安全
│   │   ├── report_service.py     # 一键自包含 HTML 运行报告
│   │   ├── part_writer.py        # 分批落盘与实时快照（断点安全写）
│   │   ├── housekeeping.py       # 运行记录/孤立文件/缓存的自动清理
│   │   ├── data_analysis.py      # 通用数据清洗服务（Analysis 节点）
│   │   ├── visualizer.py         # 通用可视化服务（Visualize 节点）
│   │   └── execution_history.py  # 执行历史记录（history.db）
│   ├── utils/
│   │   └── helpers.py            # 工具函数
│   └── static/                   # 前端静态文件
│       ├── index.html
│       ├── css/style.css
│       └── js/
│           ├── app.js            # 入口（i18n fetch 注入、节点库）
│           ├── canvas.js         # 画布与节点渲染
│           ├── workflow.js       # 执行、文件管理、全部对话框
│           ├── menu.js           # 顶栏菜单状态
│           ├── stats.js          # 情感/倾向性 ECharts 统计
│           ├── custom-select.js  # 主题化下拉组件
│           ├── zenviz.js         # 内嵌 Chart Studio 图表库
│           └── zenviz-bridge.js  # Studio 与画布的数据桥接
├── data/                         # 运行时数据（gitignore）
│   ├── runs.db                   # 断点续跑状态（节点行/指纹/LLM缓存）
│   ├── datasets.db               # 持久化的上传数据集与引用
│   ├── history.db                # 执行历史与趋势
│   ├── settings.json             # 运行时设置（驱动路径等）
│   ├── cookies/                  # 平台 Cookie
│   ├── exports/                  # 导出数据
│   ├── workflows/                # 工作流配置
│   ├── checkpoints/              # LLM 逐行检查点（runs.db 不可用时的兜底）
│   └── models/                   # 训练好的 ML 模型
├── docs/
│   └── crawler_notes.md          # 各平台实测记录（登录墙/翻页契约/字段来源），改爬虫前先看
├── logs/
├── requirements.txt
└── README.md
```

## 节点类型

| 节点 | 标识 | 说明 | 能否独立运行 |
|------|------|------|------|
| Name（命名） | NAM | 工作流元数据，其标签作为工作流名供历史/运行记录归组 | 是（纯元数据） |
| Data Source（数据源） | SRC | 从知乎/微博/小红书/哔哩哔哩/抖音/YouTube/X（推特） 按关键词采集，知乎、YouTube、X（推特）、哔哩哔哩 与抖音 另可选「某作者的作品」（知乎填主页链接或 /people/ 后面的 id，YouTube 填 @handle/频道链接/UC… ID，X 填 @handle 或主页链接，哔哩哔哩填 space.bilibili.com/<UID> 链接或数字 UID，抖音填 douyin.com/user/… 链接或那串 sec_uid），哔哩哔哩 另有**热榜**（热门榜分页 / 周排行榜一次回整张表，均不需逐条请求），微信按粘贴的推文链接采集正文；「采集内容」切到**评论**即变为评论采集器。可选项完全由后端 `/api/capabilities` 的采集矩阵生成，新增平台或模式不必再改前端；**运行前的本地校验也读同一份矩阵**（以前它自己写着"评论要链接、微信要链接、别都要关键词"，于是 4 个「某作者的作品」模式与 B 站热榜在界面上根本跑不了——填好了也报「缺少关键词」，请求都不会发出） | 是，需平台+关键词/作者（或评论/推文链接；热榜只需选榜单） |
| Upload（上传） | UPL | 从持久化数据集中读取 CSV/TSV/JSON/TXT/Excel 作为输入 | 是 |
| Process（处理） | PRC | LLM 语义清洗 / 情感(LLM/ML) / 倾向(LLM/ML) / 关键词 / 聚类 / NER(规则/LLM，可指定实体类型) / 异常 / 相关性 | 否，需要上游文本数据 |
| Analysis（分析） | ANL | 确定性数据清洗：去空/去重/筛选/改名/类型转换/排序/采样/分组聚合/表关联/列计算/分箱 | **是**，可直接处理数据集 |
| Visualize（可视化） | VIZ | 柱状/折线/饼图/散点/直方/箱线/热力/桑基/词云/地图，ECharts 或 Matplotlib | **是**，可直接处理数据集 |
| Tokenize（分词） | TKN | jieba 分词输出词频，供导出或词云使用 | 否，需要上游数据 |
| Output（保存） | OUT | 导出为 CSV / JSON / Excel / TXT / HTML / Markdown | 否，需要上游数据 |
| Resume（续跑） | RSM | 收养历史运行的节点输出作为数据源（原上游不可用/已付费时） | 是 |
| Comment（评论采集） | CMT | 评论采集引擎（现为数据源的「采集内容=评论」模式）；独立节点仅保留给旧画布 | 是 |

## API 参考

### 工作流

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/workflow/save` | POST | 保存工作流配置（含命名） |
| `/api/workflow/load` | GET | 加载指定工作流 |
| `/api/workflow/list` | GET | 列出所有已保存工作流 |
| `/api/workflow/delete` | POST | 删除工作流 |
| `/api/workflow/execute` | POST | 执行工作流；已有运行在跑则排队（返回 `queued` 与位次；`queue:false` 保留旧的直接拒绝）。「继续」一定带 `queue:false`——排队期间它指向的运行记录可能被保留策略清掉，那时宁可立刻 400 说明原因，也不会悄悄变成一次从零重爬 |
| `/api/workflow/stop` | POST | 停止执行（浏览器在后台关，请求立即返回；`browsers` 回执本次关掉的浏览器数） |
| `/api/workflow/status` | GET | 获取执行状态（含 `queue` 等待列表；`stopping`/`settling` 说明"已叫停但结论还没写"，浏览器据此续读而不是抢答） |
| `/api/workflow/queue` | GET | 列出排队中的请求 |
| `/api/workflow/queue/cancel` | POST | 按 `id` 取消一个排队请求（绝不触碰正在跑的运行） |
| `/api/workflow/queue/clear` | POST | 清空队列 |
| `/api/workflow/processes` | GET | 列出执行器/浏览器子进程 |
| `/api/workflow/processes/kill` | POST | 强杀残留进程 |

### 运行记录 / 断点续跑

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/runs/resumable` | POST | 查询指定工作流最近一次可续跑的中断运行 |
| `/api/runs/list` | GET | 分页列出运行记录（每条带 `workflow_name`（并行时为全部工作流名，` + ` 连接）、`wf_count`、`mode`、`headless`，面板据此打「并行 ×N / 串行 ×N」与「无头 / 窗口」标） |
| `/api/runs/status` | GET | 单条运行的节点级状态 |
| `/api/runs/stats` | GET | 全局运行统计 |
| `/api/runs/purge` | POST | 按保留策略清理旧运行（中断的最后清理） |
| `/api/runs/discard` | POST | 丢弃一次中断运行（不再提供续跑） |
| `/api/runs/delete` | POST | 删除指定运行 |
| `/api/runs/<run_id>` | GET | 运行详情（含节点行数据摘要） |

### 数据与数据集

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/data/upload` | POST | 上传 CSV/TSV/JSON/TXT/.xlsx/.xls 文件并注册为持久化数据集（未知扩展名→400，不再当 CSV 猜） |
| `/api/data/paste` | POST | 将粘贴的 JSON 数组注册为数据集 |
| `/api/data/datasets` | GET | 列出已注册数据集 |
| `/api/data/datasets/<id>` | GET / DELETE | 查看 / 删除单个数据集 |
| `/api/data/inspect` | POST | 查看数据集的空值/类型/重复行统计 |
| `/api/data/preview` | POST | 分页查看数据集的原始表格（数据预览面板）；按 `node_id` 取历史行时须带 `workflow_name`——`node-2` 在每个画布上都存在，只凭 id 不猜（重启后没有身份就明确回答"无记录"） |
| `/api/data/clear` | POST | 清空内存中的数据集缓存 |

### 分析 / 可视化 / 导出（独立于工作流，可单独调用）

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/analysis/run` | POST | 对数据集执行清洗流水线，返回新数据集 id 及报告 |
| `/api/analysis/train` | POST | 从已有 LLM 标注数据集训练传统 ML 模型（情感/倾向） |
| `/api/visualize/render` | POST | 对数据集渲染图表，返回 ECharts option 或 Matplotlib 图片 |
| `/api/export/save` | POST | 将数据集导出为指定格式文件 |
| `/api/exports/list` | GET | 按时间倒序列出导出文件（名称/类型/大小/可下载标记）与目录合计 |
| `/api/exports/download` | GET | 按**文件名**下载某个导出文件（越界名→404，脚本类扩展名→404） |
| `/api/exports/delete` | POST | 删除单个导出文件（不接受路径、前缀或递归） |
| `/api/report/generate` | POST | 生成自包含 HTML 运行报告（本次运行的表格，或按 `run_id` 读历史运行；可选 AI 结论） |
| `/api/report/view` | GET | 按名查看报告：仅接受 `report-` 前缀的 `.html`，响应带禁脚本 CSP |

### Chart Studio

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/studio/dataset` | POST | 把画布任意节点/数据集的完整表格交给 Studio（支持多源合并） |
| `/api/studio/sources` | POST | 预检哪些节点当前有可用的表格数据（与预览同一套取行规则：带 `workflow_name`，不按裸 id 猜） |
| `/api/studio/save-image` | POST | 把 Studio 成品图保存为 PNG 到导出目录 |

### 统计与历史

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/stats/emotion` | GET | 情感统计 |
| `/api/stats/tendency` | GET | 倾向性统计 |
| `/api/stats/summary` | GET | 执行汇总统计 |
| `/api/history/runs` | GET | 列出所有已记录的工作流执行 |
| `/api/history/series` | GET | 按工作流/指标查询时间序列（执行历史面板趋势图） |
| `/api/history/clear` | POST | 清空执行历史记录 |
| `/api/history/delete` | POST | 删除**单次运行**的执行历史（`{run_id}`）；返回 `deleted` 条数，0 表示这条运行已不在历史里（可能被保留策略清掉了），不谎报成"已删除"；不带 `run_id` 一律 400 |

### AI / Cookie / 系统

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/llm/models` | GET | 拉取 OpenRouter 免费模型列表 |
| `/api/llm/ollama/models` | GET | 列出本地 Ollama 已拉取模型 |
| `/api/llm/test` | POST | 测试所选提供方的连通性 |
| `/api/cookies/status` | GET | 各平台 Cookie 是否存在 |
| `/api/cookies/flow` | GET | 每个平台 Cookie 的用途、获取步骤、默认登录页与可接受域名（Cookie 面板据此渲染） |
| `/api/cookies/save` | POST | 保存指定平台 Cookie（不属于该平台的域名条目会被剔除，一条都不剩时直接拒绝而不是存个空文件） |
| `/api/cookies/generate` | POST | 打开浏览器引导扫码登录并捕获 Cookie；可选 `url` 指定入口链接（仅限该平台域名），取 Cookie 前先把浏览器带回本平台页面 |
| `/api/cookies/verify` | POST | 用已存 Cookie 实地探测该平台还放行什么（结论逐条回显：可用 / 仍被挡在登录页 / 回的是验证码·风控所以**没有结论**） |
| `/api/cookies/delete` | POST | 删除该平台的 Cookie 快照文件；回话里说清该平台的浏览器 profile 是否仍持有登录态（删文件不会把 profile 登出） |
| `/api/cookies/preflight` | POST | 运行前一次性验证画布要用到的多个平台：`{platforms, use_profile?, fresh?}` → 逐平台 `valid/expired/unknown/nocookie/nologin/notcrawlable` + 已渲染文案 + `blocked`/`unclear` 两个清单（结果按 `COOKIE_PREFLIGHT_TTL` 缓存） |
| `/api/settings` | GET / POST | 读取 / 修改运行时设置（data/settings.json） |
| `/api/browser/profiles` | GET | 每个平台的持久浏览器 Profile 状态（是否启用、目录是否已建、是否已导入过 Cookie、占用空间、是否建议开启） |
| `/api/config` | GET | 系统配置 |
| `/api/capabilities` | GET | 采集矩阵：每个平台支持哪些采集模式、每种模式要填什么（数据源面板整块由它渲染，中英词条用 key 送出） |

## 使用示例

### 1. 从画布创建完整工作流

1. 从左侧节点库拖拽 "Data Source" 到画布
2. 双击节点打开设置面板，选择平台和关键词
3. 添加 Process 节点 (Clean / Emotion / Tendency)，在 AI 面板选好提供方与模型
4. 添加 Analysis 节点做进一步的确定性清洗（如去空值、去重）
5. 添加 Tokenize 节点产出词频（可选，供词云/导出使用）
6. 添加 Output 节点选择导出格式，并添加 Visualize 节点查看分布
7. 拖拽连线连接节点，加一个 Name 节点命名工作流
8. 点击菜单 "Execute" 运行

### 2. 独立使用 Analysis / Visualize 节点（不采集，只处理已有数据）

1. 直接在画布上拖一个 Visualize（或 Analysis）节点，不连任何上游节点
2. 打开节点设置，把 "Data Source" 改成 "Upload File"，上传一份 CSV/TSV/JSON/TXT/Excel
3. 配置图表类型 / X、Y 字段后点击 "Preview Chart" 即可在右侧预览面板看到结果，
   不需要跑整个工作流；上传的文件会持久化，下次打开这个工作流还能用

### 3. 中断后续跑（断点续跑）

1. 执行中点击 Stop、直接关服务、或爬虫/LLM 中途失败——都没关系，
   已完成的节点和已处理的行都记录在 `data/runs.db`
2. 重新打开该工作流，画布顶部出现"检测到中断的运行"横幅
3. 点「继续」：未变化的节点直接复用检查点（LLM 结果走缓存不重付费），
   只重跑缺失部分；改了某节点参数则该节点及其下游自动失效重算
4. 点「重新开始」则忽略检查点全量重跑；不想要这次续跑可在运行记录面板「丢弃」

### 4. 用 Resume 节点接管旧运行的数据

原平台已登出/风控、或那批数据已经花过 LLM 成本时：拖入 Resume 节点，
选择某次历史运行及其节点，它的输出行就直接成为当前工作流的输入。

## 配置

### 环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `OLLAMA_MODEL` | Ollama 模型名 | `qwen3.5:9b` |
| `OLLAMA_HOST` | Ollama 服务地址 | `http://localhost:11434` |
| `SECRET_KEY` | Flask 会话密钥 | `crawler-workflow-secret-key` |
| `PORT` | 服务端口 | `5000` |
| `HOST` | 监听地址（默认只监听本机，局域网内其他机器访问需设为 `0.0.0.0`） | `127.0.0.1` |
| `FLASK_DEBUG` | 调试模式（`1`/`true`/`yes` 开启；含热重载与自动打开浏览器，调试器可执行任意代码，勿在他人可达的机器上开启） | 关闭 |
| `MAX_UPLOAD_MB` | 单个请求体上限（上传/粘贴数据过大时返回 413 JSON，而非把整机内存吃光） | `64` |

### 运行时设置（UI 设置面板 → `data/settings.json`）

| 键 | 说明 |
|------|------|
| `driver_path` | chromedriver 路径（覆盖 config.py 默认值） |
| `browser_binary` | Chrome 二进制路径 |
| `window_size` | 浏览器窗口尺寸 |
| `page_load_timeout` / `element_timeout` | 页面/元素超时（秒） |
| `ollama_host` | Ollama 地址（覆盖环境变量） |
| `cookie_preflight_before_run` | 运行前是否自动验证各平台 Cookie，过期就禁止执行（默认开；关掉则什么都不问） |
| `same_platform_queue` | **真排队**：同一平台一次只跑一条采集，前一条整个跑完才轮到下一条（默认开）。开启后并行同平台不再弹「用不用 Profile」确认框——这个决定已替整台程序做掉。关掉就只剩**错峰**：两条仍可能同时跑，只把发车时刻按下面的秒数隔开。**微博不受此开关影响，永远真排队**（矩阵 `serial_only`：并行翻页必撞登录墙），并行画布含多条微博时执行前单独弹窗说明 |
| `same_platform_stagger` | 同平台**错峰间隔**（秒，默认 12，可填 0~600；0 = 不额外隔）。只对**同时在跑**的同平台采集生效，串行永远无缝；真排队时它是交接后的停顿，关掉真排队时它就是全部的秩序 |
| `warn_mixed_region` | 一张画布同时爬国内与海外平台时先问一句（默认开；见「网络分区」一节） |
| `use_browser_profile` | 是否为每个平台使用持久浏览器 Profile 目录（默认开；关闭 = 每次都是全新浏览器 + 种一份旧 Cookie 快照） |
| `browser_profile_dir` | Profile 根目录，留空 = 内置 `data/chrome_profile/<平台>`；填绝对路径可指向你自己为该工具建的目录 |
| `PROFILE_LOCK_TIMEOUT` | 一个平台的 profile 被另一次采集占用时，最多排队多久（默认 900 秒；超时即以可读的原因判该节点失败，不再抛驱动栈） |

### 保留策略常量（`backend/config.py`）

| 常量 | 说明 | 默认值 |
|------|------|--------|
| `RUN_KEEP_PER_WORKFLOW` | 每个工作流保留最近 N 次运行 | `20` |
| `RUN_KEEP_DAYS` | 运行记录最长保留天数 | `30` |
| `RUN_MAX_ROWS_PER_NODE` | 单节点持久化行数硬上限 | `500000` |
| `DATASET_MAX_ROWS` | 单个上传文件行数上限 | `300000` |
| `DATASET_KEEP_DAYS` | 无引用文件的清理天数 | `90` |
| `HOUSEKEEPING_INTERVAL_MINUTES` | 自动执行上述保留策略的时间闸（启动必清一次，之后每过此时长在运行结束时清） | `60` |
| `LOG_MAX_BYTES` | 单个日志文件上限，超出即滚动 | `5242880`（5 MiB） |
| `LOG_BACKUP_COUNT` | 保留多少个历史日志（`app.log.1 … .N`） | `5` |

### 排队、错峰与预检常量（`backend/config.py`）

站点把"同一账号一秒内的两次搜索"当成异常（微博弹登录页、知乎回 40362），profile 又只允许一个浏览器，
所以这几条不是性能参数而是正确性参数：

| 常量 | 说明 | 默认值 |
|------|------|--------|
| `SAME_PLATFORM_STAGGER` | 同平台两条**同时在跑**的采集至少隔多久发车（默认值；串行永远无缝；面板里 `same_platform_stagger` 可改，真排队时它是交接后的停顿，关掉真排队它就是全部的秩序） | `12.0` |
| `WALL_RETRY_BACKOFF` | 第 0 行就撞墙时的退避重试等待；0 关掉重试（有过半数据的墙不在此列，那是 Cookie 死了，走续跑） | `45.0` |
| `PLATFORM_GATE_TIMEOUT` | 一次采集最多等多久轮到本平台的同平台排队 | `900` |
| `COOKIE_PREFLIGHT_TIMEOUT` | 运行前自动验证 Cookie 的总预算（超出即回答「无法核对」，不谎报失效） | `45` |
| `COOKIE_PREFLIGHT_TTL` | 一个验证结论可被复用的秒数；保存/删除/扫码取回 Cookie 会立即作废 | `300` |
| `COOKIE_PREFLIGHT_MAX_PARALLEL` | 预检同时最多开几个浏览器（每个都是一次真实页面加载） | `4` |

## 训练传统 ML 模型

情感分析和倾向性分析支持传统 ML 模式（sklearn TF-IDF + 逻辑回归），需要先用 LLM 标注一批数据来训练：

1. 在工作流中配置 Process 节点 → Emotion 或 Tendency → 保持默认 LLM 模式
2. 连接上游数据源，执行工作流
3. 完成后，将 Process 节点的模式切换为 `ml`，点击「训练 ML 模型」
4. 系统自动从上游节点结果中提取文本列和标签列训练模型
5. 训练完成后，后续执行将使用 ML 模式批量推理（无需 Ollama / 不耗 token）

ML 模型保存在 `data/models/` 目录下，训练一次后持久可用。

## 开发与验证

- 依赖安装、lint（ruff + pylint）、启动与测试均使用项目虚拟环境 `.venv/`
- 代码风格由 `ruff.toml` 约束（行宽 120、单引号），提交前必须通过
  `ruff check` 与 `ruff format --check`，且只允许真正修复，禁止 `# noqa` 式忽略

### 自动化测试（pytest，快速层 2585 用例，全层 2714）

测试体系分六层，位于 `tests/` 目录：所有写入都落在临时目录（绝不触碰真实 `data/`）；
`live_site` 层会真实读取 `data/cookies/` 里的登录态去访问目标站点，而 `live_quick`
是它的一个子集（每个可爬平台各一条真采集），用来在每次改动后把关——51 条真爬要跑一个多小时，
没人会在每次改动前跑它，而一道没人跑的闸门等于没有闸门：

| 层 | 内容 | 外部依赖 |
|----|------|----------|
| `tests/unit` | 纯逻辑：断点续跑存储（含**进程被硬杀后按已落库的行回填「已存行数」与完成数**——那一列只有收尾那次写入会更新，正是被跳过的那次；什么行都没保住的节点判 failed 而不是 partial，与执行器同一口径；`node_done` 只数真正完成的节点，restored 算完成；**改工作流名字不是一次新计算**——标签进过节点指纹，而父指纹喂给子节点，于是改一个名字让整条链作废，还顺带移动了去重台账的作用域（`'item:' + 节点指纹`），续跑就把已采过的条目整批重爬重付；**续跑要改写记录的描述字段**（mode/无头/工作流名/结构指纹/语言，`started_at` 不动）；**被保留策略裁剪的运行交回它的"已采集"认领**（那些行已被删除，留着认领等于让以后同一次采集静默少给条目，而手动删除仍保留认领））、数据集存储、DAG 引擎（另含**悬空连线不再被报成"存在环"**、**空画布一律拒绝而不是"完成 0/0"**）、执行器（另含**一层任务必须等它自己启动的线程全部退出才交回运行线程**——被停止的那层以前 `shutdown(wait=False)` 就走，而它的调用者紧接着释放运行槽；`stop()` 也不再顺手把池句柄置空，那会让真正拥有池的那层在收尾时抛 `AttributeError`）、14 种清洗算子、10 种图表、6 种导出（**导出文件名的时间戳写在扩展名之前**，名字被占用就退让成 `-2`/`-3`，且永不返回一个已存在的名字）、分析器、i18n、浏览器启动参数（**图片偏好必须是明确答复**：抓取写"封锁"、人眼窗口写"允许"，只"不写这个偏好"会继承 profile 里上一次抓取的封锁——这正是登录窗口扫不出二维码的原因）、**导出目录读取与路径规则**（子目录不遍历、符号链接指向外部即拒、被占用的文件不误报成功）、**日志轮转**（滚出 .1…​.N、上限、纯追加退路）、**保留策略时间闸**（首发/窗内跳过/一个库报错不阻断另一个）、**Cookie 入口链接白名单**（伪后缀、userinfo 夹带、scheme 注入一律拒）、**微信能力边界**（诊断只剩通用的 {platform,url,login_wall} 三键，源码内不得再出现 searchbiz/appmsg/fakeid；批量抓取逐篇只播一行——`get_detail` 里不得有 info 级日志，任何微信消息不得承诺 阅读/点赞/打赏 这些它取不到的数字，也不得有 `--- `/`>>> ` 装饰）；**目录的 AST 门**（`t('key', …)` 的每个调用点必须填齐该模板的占位符——缺参数时 `t()` 有意原样返回模板而不在爬取中途崩掉，于是漏一个关键字就是屏幕上多出一行 `阅读={reads}`；parity/可达性/中英对齐对这种错误全部沉默，因为它钉的都是"键存在且被引用"）；**前端 JS 行为层**：`tests/frontend/*.mjs` 用 node 直接加载真实的 canvas.js / workflow.js / app.js，测 validate 预检门（**含"数据源要什么由矩阵说"这一条**：B 站作者模式与热榜填好必须 0 错、抖音作者字段空着必须点名 author 字段、微博没有作者模式必须按名字拒而不是要关键词、矩阵没加载时必须说"没法核对"而不是瞎猜一个字段——把改动前的 workflow.js 拿去跑同一批场景，作者与热榜两条都会拿到「缺少关键词」）、**键盘归属**（选中节点后在多行框里按 Backspace 不得删掉节点、输入 f 不得被折叠快捷键吞掉——旧实现在 TEXTAREA 里照删不误），**设置面板与快照**（`harness_state.mjs` 的 paramEdits/autosaves/initialPush 场景：撤销必须回得到上一个参数值、60 次自动保存不得挤掉真实历史、打开 5 节点草稿只留 1 个撤销点）、**数据集重命名送出的是用户输入而非按钮记号**（同时断言对话框规格里主按钮不带 value——带了我就会把 'ok' 存成文件名）、**打开文件要读回全部四项运行设置**（parallel/headless 两个方向都得生效，否则打开一次就把文件改错）、**stats.js 的缺库降级**（`harness_stats.mjs`：无 echarts 时 init/refresh/renderPie 都不抛、提示以元素形式渲染、有库时两张饼图照常建立）、**数据源面板的采集矩阵驱动层**（`harness_capabilities.mjs` 用后端当场导出的矩阵当载荷：没载荷/载荷是废的/平台没声明/字段名不是标识符/按钮指向不存在的函数，五种答复各测各的；新画布节点的默认值必须逐项等于矩阵声明的默认值）、**节点参数只是数据、不是标记**（同一批设置面板再用「一个引号就能逃出 `value="…"`」的载荷重画一遍：20 个面板每个自由文本字段都必须以转义形式出现、且原样标签一个都不许落到 HTML 里——旧写法把 `p.text_column`/`p.title`/`p.x_field`/进程名等 20+ 处直接拼进面板，改一下工作流 JSON 就能在打开节点时执行脚本；另外断言"确实投毒成功了"，否则面板把字段整个删掉也能算过；续跑面板由 await fetch 填充、本 harness 画不到它，就钉它写 HTML 的那三行）、**切换语言必须重画 JS 拼出来的表**（`sandbox.setLang('en')` 之后，数据集表与执行历史表的表头要真的换成英文、旧语言的词一个不剩——`harness_panel_actions.mjs` 用两条 route 喂数据，读的是产品自己赋的 `innerHTML`；这两处以前一个有方法没人调、一个连方法都没有）、**粘贴项只在剪贴板有东西时出现**（复制前后各测一次）、弹窗外点关闭、目录双语对齐、undo/redo、工作流保存/打开/新建全链路、运行记录面板转义与按钮、**运行控制台的游标与标签页历史**（`harness_console.mjs` 一格一格喂真实的 `pollStatus`：首屏全给、二屏只补缺、服务端缓冲被下一次运行清零时必须从头读懂（曾经"第二次运行后控制台永久冻结"）、超过 200 行时增量只能来自 total 而不是数组长度、清空之后只播下一行不重播缓冲、**切走再切回必须原样看到自己那几行且下一格不重复**、没被看过的标签页打开时不得空着、乙的行不得混进「全部」）、**导出产物面板**（不可下载项不给下载按钮、文件名同时做 HTML 与 JS 双重转义、大小读数 B/KB/MB、表头按目录合计而非分页计数、空目录是空态而非报错）、**Cookie 面板**（按平台渲染步骤、切换不重复堆叠、入口链接随请求送出、被拒链接提示、验证中隐藏登录按钮、重开面板接管在跑的任务、**删除按钮先问再动**：取消时一个请求都不发、确认时送的是下拉框此刻选中的平台、删除后必须顺手刷新状态、被拒时不假装成功、profile 警告原样转达；`showDialog` 规格断言按按钮 value 判定，因为带输入框的对话框是"输入框优先"的）、**运行前的 Cookie 闸门**（`harness_cookie_gate.mjs` 跑真实的 `workflow.execute()`：不爬任何东西的画布一个请求都不发、续跑永不被拦、平台清单来自数据源节点**和**评论节点链接的域名（去重按节点顺序）、失效就**不发**运行请求且弹窗点名平台并引用后端原话、**没有任何"照样跑"按钮**（按取消和按去更新一样拦下）、"无法核对"只提示不拦、预检本身失败时如实说明这不是通过、开了自动验证才逐平台探测、关掉时什么都不问静默放行、以及**逐次运行的 profile 答复要原样送去预检**（选"本次不用"传 false、没得选时字段整个缺席而不是替用户改成 false）；这条链上还抓到一个真 bug：app.js 把 `openCookieDialog` 包一层加拖拽/缩放，包装函数丢掉了参数，于是"把你带到出问题的那个平台"永远停在上次选中的平台）、**画布指针交互层**（拖出临时连线、自环与重复连线被拒、落空取消、分词↔可视化连线强制 word_freq、一条连线一个悬停删除区且点击真删、同一帧多次调度只重绘一次、端点消失不画幽灵线、折叠、缩放上下限与像素取整、复位居中、自动布局按依赖顺序且分组件不重叠、设置面板开-关-重开与删节点不留孤面板、真实改名对话框、委托事件、右键平移阈值决定是否弹菜单、**右键菜单按自己的真实尺寸钳位**（旧的 200px 常数既可能让菜单跑到屏幕外、也可能提前把它挤回来；语言与折叠项都会改变它的宽高）、**节点绝不按 DOM id 查找**（文本门禁止 canvas.js 出现变量参数的 `getElementById`，并断言 `_nodeEl` 真的被用到）、**一次按键只出一条 toast 且里面有全部问题**（`harness_panel_actions.mjs` 造两个坏数据源跑真 `execute()`：一条消息、行数=问题数、表头写着这个数、且没有发出运行请求；只有一个问题时不得带列表头）、**折叠项的 data-action 跟着当前状态走**（右键"折叠/展开"文案换了而动作没换，就是点了没反应）、从草稿 init 恢复含损坏草稿）、**应用状态层**（`LLMSettings.payload()` 这个运行请求体的真正组装者首次被执行：两种传输各存各的模型、选 Ollama 绝不带 OpenRouter 的 key、未知 provider 归一化、旧扁平键一次性迁移、坏草稿仍可用；RunState 存-写-刷新且 running 不落盘；Settings 草稿的保存与回填改用真实词典断言 `[data-i18n]` 盖章；AppSettings 拉取/单飞/强制刷新、复选框按 checked 回填、只提交改动过的字段、失败与警告都要让用户看到）、**面板动作层**（横幅出现并写清已完成节点与已存行数、继续送出 resume_run_id、重新开始先 discard 再冷启动、运行中拦截二次尝试、中断走 discard 而完成走 delete、详情行只取一次且再点不重请求、导出/数据集/进程/顶栏每一动作的请求体与取消、**执行历史逐条删除**（每行一个按钮且 id 原样留在属性里、点「取消」一个请求都不发、确认后只送被点那一行的 run_id 并同时重载列表与序列、`deleted:0` 说成"已经不在了"而非"已删除"、被服务端拒绝时不假装刷新过）、**运行记录标签读的是 mode 不是 wf_count**（parallel×2→「并行 ×2」、serial×2→「串行 ×2」而绝不出「并行」、单工作流两种 mode 都不打模式标、无头/窗口恒打），）、**顶部菜单**（menu.js 此前没有任何测试加载过它：开关镜像、运行按钮永不禁用只改口、停止按钮的可按性、语言与背景色块的 active 标记、圆角回读与钳制、子菜单互斥与外部点击/Esc/缩放关闭）；另含 node 无关的静态契约测试（序列化字段、键目录、**Cookie 面板的三向对齐**：HTML 里的平台下拉框逐项等于后端白名单、画布数据源的平台列表逐项等于「真能爬」的那些、每个平台使用说明里点名的按钮必须是界面词典此刻的文案——按钮改过一次名，八份指南就会集体指着不存在的控件，而这正是本次实测到的状态）、**服务端设置的四处对齐**（`settings_store.DEFAULTS` 的每个键都必须出现在 `AppSettings._inputMap`、index.html 里那个控件的 id、以及 `onSettingInput('键')` 的接线里——少一处就是一个"存在但没人能改"的设置；自动验证与旧确认框必须在两种语言里互相指明对方，不然用户以为关掉了弹框却还在开浏览器）与**共享登录墙判定**（X 的 `/i/flow/login`、Instagram 的 `/accounts/login/`、Google 中转页都要认得，正文里提一次「请先登录」不算墙）与 **Cookie 归属过滤**（本站域名与它的每个播种主机都放行，登录中转站与 `evilyoutube.com` 这类伪后缀一律拦）、**浏览器 Profile 的 JS 层**（`harness_profiles.mjs` 跑真实的 workflow.js + app.js：运行前提醒的纯判定（开=不问、矩阵没到=不许乱承诺、未标记的平台不打扰、两个被标记的平台只弹一次并报数）、"继续运行"才放行而"先去设置"不开跑并真的打开面板、设置面板里每个平台一行、建议标记落在矩阵说的那些行上、服务器空答复时显示"读不到"而不是留个看起来健康的空表、打开设置面板这个动作本身就是读取时机、数据源面板的提示语随开关换句） | 无（JS 行为层需 node，缺 node 自动跳过） |
| `tests/api` | Flask test_client：覆盖 50+ 端点，含「上传→清洗→导出」完整执行链路、Cookie 登录状态机、**Cookie 用途接口与验证任务**（无 Cookie 直接拒、越界链接拒、探测崩溃判 error 不挂面板、登录按钮不得应答验证、验证与登录共用单飞锁、**验证有三种结论**（回验证码/风控必须说"没有结论"，既不能算可用也不能算失效）、**运行前 Cookie 预检**（逐平台判定与缓存：只有登录页算失效；风控、超时、驱动起不来、profile 被占用一律"无法核对"且不拦；无 Cookie 也无 profile 才算"没东西可测"并拦下；已用过的 profile 即使没有 Cookie 文件也值得探测；结论缓存 TTL 内不再开第二个浏览器、`fresh` 强制复检、只缓存 valid/expired 两种；保存/删除 Cookie 与扫码取回都会作废缓存；越界平台名整体拒绝而不是悄悄丢掉；探测必须走可见窗口且绝不把浏览器实时 Cookie 回写文件；并行上限是真的（上限 1 时两个平台确实不重叠）；超时的平台仍出现在答复里且不算拦截；文案按请求语言渲染）、**删除已存 Cookie**（删除只动文件、profile 仍登录时回话说清并多一行；没得删是 404 而不是假装删过；一次删除只播一行；删完还能重新存回）、**Cookie 中途过期→节点判失败→换 Cookie 续跑不重复**的完整模拟、**十类节点逐一执行验证（分词/图表/导出格式/续跑收养）**、**节点×算子×参数功能矩阵**（处理节点的关键词/聚类/相关性/情感/倾向/异常检测、分析节点 14 个清洗算子含 join 的第二入边与分箱的两种 bins 写法、分词三种输出模式与 `top_n=0` 的真实语义、十种图表走节点与走 `/api/visualize/render` 两条路、输出节点每种受支持格式与文件名越界清理、**输出节点「文件名追加本次运行时间」**（一次运行落一个
带时间的名字且绝不写出未带时间的那个、同一秒内的第二次运行各留一份、时间戳不进节点指纹（进了就等于
每次续跑都认不出自己）、不勾选时仍是旧的一次覆盖一个文件）、评论节点 `per_article_file` 与 `comment_limit` 边界、数据源「重新采集」释放台账、以及面板能产出而单测没覆盖的边界值 `0/-5/abc/''/1e400`）、**队列交棒的四种结局**（占到槽、槽被抢所以退回队首、永不可能启动的继续直接丢弃、坏载荷不拖住后一条）、**被停止的并行运行不得把节点线程留给下一次运行**（三条工作流并行、各自停在节点里；交回运行槽的那一刻是否还有线程在节点内。只有"第一个组件已经返回而另一个还挂着"这一种时序看得见那个分支，所以测试按 `_wf_local.idx` 认出第一个组件、只放行它——把 `wait=True` 回退成 `wait=False` 即红）、**21 个「平台×采集内容」组合的派发矩阵**（`tests/api/test_source_dispatch_matrix.py`：每一个组合都真走一遍执行器，断言命中的是不是矩阵点名的 handler、参数与 `crawl_kwargs` 逐项相等（不许多不许少不许被默认值悄悄替掉）、评论模式必须交给评论引擎而不是平台自己的正文抓取；矩阵点名的爬虫方法与 `CommentSession.crawl_<平台>` 适配器必须真实存在——作者与热榜模式此前从未被派发到过，因为套件里每个假爬虫只有 `search`）、以线程存活而非 running 标志决定的占用、正在写的运行不得被丢弃/删除、保留策略对实活运行的豁免、存储打不开时运行仍干净收尾并释放槽位、改过节点参数后续跑必须重算该节点、**连点两次「继续」**（第二次仍然复用上一次的复原结果——判定只认 done 的话，续跑的省成本只兑现一次点击）、**已从画布删除的节点不得让后续运行一直失败**（结束行与运行状态只数这次真跑过的节点）、跨进程重启续跑、**并行运行的记录与续跑**（一条画布跑两个工作流＝一条记录但两个名字都在 `workflow_name` 里、重名的两个工作流名字只写一次而 `并行 ×2` 仍成立、单工作流的记录名字一个字符都不能变因为它是查旧行的键、旧数据库缺 `wf_count` 列时打开即补且旧行读作 1、失败的那条工作流不得连累另一条重跑且改动只重算自己——两条工作流各自取回自己的行）、**名字里含 `+` 的工作流不得借用别人的行**（查旧行时先按浏览器显示的整串名字精确匹配，只有落空才放宽到组合名的片段——否则一条真叫「节奏」的画布会读到「节奏 + BPM」那次运行的表）、**无头/窗口与平台清单**（`/api/browser/profiles` 只列能取 Cookie 的平台，微信因不需要登录而不在表内）、（`/api/history/delete` 只删那一次运行的指标、其余历史与趋势图不动、名字列表里不再留下没有行的工作流、`deleted: 0` 如实回答"已经不在了"而不是谎报删除、缺 run_id 一律 400 且不动任何行）、**并行/串行 × 同平台/不同平台 × profile 开关的运行矩阵**（真锁而非假桩：并行同平台 + 用 Profile → 两次采集的生命周期零重叠且两个节点都 done；+ 不用 → 确实重叠；并行不同平台 → 必须重叠（否则就是凭空加的成本）；串行 → 无竞争；没送答案 → 工厂收到 None 并按设置解析出真实目录；送 False → 解析出 None；**「同平台排队」开关本身也是矩阵的一列**：开→即使不用 profile 同平台两条采集也不重叠、且排队只在控制台播一次，关→真重叠；另含**撞墙重试的四种形状**（第 0 行就撞→退避后重试一次并播一行、已采到行才撞→绝不重试（那是 Cookie 死亡，走续跑）、带着游标来→绝不重试、运行已被停止→绝不重试，以及重试前必须清掉上一次那面的墙标志，否则节点会谎报一个没撞到的墙）；同一画布串行跑时记录存 `mode=serial` 而 `wf_count` 仍是 2——标签读的是 mode 不是数量）、**控制台噪声与事实的分界**（命名/上传两个节点不再播报"正在执行节点"与"节点完成 n/N"，但"已载入 xxx.csv：N 行"照旧、文件丢失时节点仍判失败且原因带节点标签——压噪声不得压掉可诊断性）、**拒绝必须让节点失败**（未知处理算子与分词的三种拒答以前 `return []`：节点照 done、运行照绿、导出一个只有表头的文件，且唯一那句解释不带节点名，于是所有健康节点都被指认为凶手；现在一律 raise，原因随 `节点 X 执行失败` 送出且只送一次）、**一次失败只播一遍**（LLM 传输死亡以前是三行——本模块 say、run_llm_dataframe 再 logger.error、执行器再节点失败——而熔断文案本身就有两句；现在只有带节点名的那一行留下，`llm.aborted` 只报"保住了几行"；逐行进度与逐批进度同一时刻只有一个在说话，被稀释时批标记才出声）、**控制台行缓冲自身**（`tests/unit/test_console_buffer.py`：超过 200 行时裁剪与 `_log_total` 必须同步、多行错误按物理行逐条入缓冲（驱动堆栈以前算 1 条而 DOM 画 3 行，游标就错位）、空消息不占行；**控制台基线只有一份定义** `app.reset_console_state()`，运行开始与测试隔离共用它——直接调用执行函数的测试不经过运行开始，也就没人替它清屏，两处各写一份内联重置就是"上一次运行的 25 行日志出现在三个文件之外的断言里"；外加**模块级 `import app` 的门**——collection 早于任何 fixture，那样导入了 app 就会把它的单例服务绑到用户真实 `data/` 上，本轮就真的把测试名字写进了你的执行历史）、**串行运行的标签页归属**（组件之间必须交还线程局部的 wf 索引，否则「开始执行工作流 B」会被写进 A 的标签页缓冲）、**评论 Cookie 失效点名真被撞的平台**（以前硬编码四平台列表，爬 YouTube/X 被撞墙时报告里没有一个名字是对的）、**续跑节点用「标题 #id」说话**、**排队位次不借用"未命名工作流{i}"的模板**（那个 {i} 是画布上的组件序号，不是队列深度）、**导出产物三端点**（列表分页与合计分离、下载按名解析、`../` 与绝对路径与子目录一律 404、删除越界名不生效、畸形请求 400）、**停止（空闲/运行中）→记录判中断且数据保全、线程强杀保护主线程、卡死浏览器按 PID 定向回收**、设置面板布尔开关、API 加固（28 条带体路由逐条点名，漏一条就红） | 无（外部调用全 mock） |
| `tests/integration` | LLM 传输边界（OpenRouter 一律 mock，绝不真实请求）、行运行器（断点/熔断/取消）、真 Chrome 解析 `file://` 夹具、**Profile 会记住抓取的图片封锁**（同一个临时 profile 先按抓取跑一次：
页面上那张 1×1 PNG 的 `naturalWidth` 必须为 0，且 Chrome 自己写下的 `Default/Preferences` 里必须真的留着
`images: 2`——这条"会残留"的前提一旦哪天不成立，测试就指名要求复核代码，而不是让修复悄悄失去依据；
再以登录窗口开在**同一个**目录里：图片必须加载得出来），微博/知乎假驱动登录墙判定、**登录页"路过型闪现"不得钉死判墙**（假驱动每次读取给出下一页：先 passport 再回内容页 → `login_wall` 必须仍为 False；一直停在 passport → 必须判墙；而**干净落地的页面只许被读一次**——逐条详情抓取每次都付这个钱，所以等待只对"疑似墙"开销）、**小红书导航契约**（renderer 超时是本页自己回答的"没就绪"，而不是把节点带崩的驱动堆栈；抛掉的那次请求仍要记进 `requests` 成本账；慢页面不等于 Cookie 死了）、**"绕开 `Crawler.open` 的导航"是一张只能变小的账**（逐文件数出仍以裸 `driver.get` 进页面的位置并钉死条数：新增一处即红，改走 `open` 之后不清掉条目同样红）、**矩阵里发出的每个字段都必须真的被爬虫读到**（`crawl_kwargs` 按关键字送参、而爬虫方法都以 `**_kwargs` 收尾，所以面板上一个没人读的开关会被静默吞掉——这条测试当场指出小红书的 评论预览 是从 kwargs 袋子里显式取的，于是"显式形参"与"模块内写出该 key"两种写法都算已读）、**知乎正文展开契约**（假卡片自带三种「阅读全文」形态：就地重画同一个节点 / 点了没回应 / 专栏卡一碰就跳走——最后一种在假驱动里直接判失败，所以"绝不点专栏卡"是被证明的而不是一句注释；展开之后**只有正文被换掉**，发布时间与作者仍是列表原本报的那一份（实测同一次点击会把日期挪去 `.ContentItem-time` 并去掉正文开头的 `作者名：`）；连吃三次没回应就本次不再付费并把话说明；勾选关掉时一次都不点、改为说明整列都是摘要；锚点没挂上的卡种类未知，同样不点）、**哔哩哔哩假驱动翻页契约**（首页不带 `page=`、广告/课程卡不耗请求、`-404` 跳过而非停摆、非 `-404` 的拒绝在零结果时raise 可执行错误、游标按页记录并可续页）、**抖音假驱动契约**（结果页只走 `/search/<关键词>` 路由、绝不残留旧的 /jingxuan 入口、**骨架行不算结果**（等锚点而不是等节点，永不填充→合法 0 行且不付费访问）、窗口滚动确实翻页、验证码标题→可执行错误而非空表、弹窗在到达时就被我方清掉、行数只带自解释的四个计数、**断言行里没有播放数列**、已开过的视频不重复付费、达标后不再多滚一轮、**作者模式只跳容器不滚窗口**（滚窗口=0 次容器跳转，否则页脚推荐视频会假装成作品在翻页）、**作品数为空不算 0 作品**（-1 与 0 是两句话：前者是页面没给数字、必须拒；后者是这个人真没发过）、**续跑必须走过自己已付过的那一屏**（旧实现在"屏上 id 全认识"处直接收尾，等于把死掉那次的行交回来 yet 目标没满也不报错——搜索与作者两条路同时被这个共享循环修好）、评论滚动去重）、**X 假驱动契约**（进度只认「保住的行」——三种屏各 2~3 张卡、id 轮换，按节点数判进度会在第一屏就收工；游标必须把 **id 集合与本次关键词**一起记下，否则续跑不知道自己爬过什么；一张卡恰好一次 `execute_script`，整屏最多两次列表查询，任何按字段回查定位器的写法即红；登录浮层与 403 都 raise 可执行错误而不是交 0 行，而「真的没人发」是合法的 0 行；回复用同一条走法，根推文自己不得作为回复入表，未渲染=死链、渲染了但只有根推文=未开放评论）、**真浏览器 UI 回归：自启服务器后逐容器 × 中英双语实测「容器不得溢出自身盒子、页面级不出水平滚动条」**（20+1 个顶层容器 × 双语，每个容器都带一个在真 Chrome 里数出来的元素数下限，其中 `#profile-status` 是异步渲染的那一类——检查前要先等它把行画出来，否则量一个空盒子也算过；并显式拒掉「id 不存在就退回量整页」的退化——这两条加进来当天就抓到一个不存在的 `#settings-panel` 一直在替整页体检背名、以及 `.panel` 这个不存在的类让窄窗口检查扫了 0 个元素却常绿；声明可滚的容器单独放行，但其中被截断的单元格必须带省略号——静默裁掉等于让用户读不到数据；表单控件与 `option` 自身的文字滚动不算截断）、**并行运行记录在真浏览器里的样子**（用真实的 `runsManager.render` 画一条 `wf_count=2` 的记录，不写服务器：合名两个都要看得见、两枚标签都要在、标签自身的字不得被裁、名字格不得跑出视口、页面不得因此出水平条；**并行与串行两种 mode 各测一遍，并断言"不得出现另一种说法"**）**执行历史那一行的删除按钮在真浏览器里点一次**（按钮是委托出去的，只有真点击能证明它有效：行表每次重载都会重建，逐行绑 listener 的写法正是这里出错的样子；送出的必须是所点那一行的 run_id；删除列不撑出页面级水平滚动条、按钮文字不被裁断；fetch 全程打桩——本层绝不写服务器，真删会抹掉用户自己的执行历史）、**控制台标签页在真浏览器里切走再切回**（「全部」→甲→乙→全部：每个标签页只看到自己的行，切回来还是原来那几行、不重不漏；这一步必须留在真 DOM 里——清屏与重画的语义在桩里和在浏览器里不是一回事），窄窗口下浮动面板不得跑到视口外，节点框宽度上限与标题省略号、标记里没有内联事件、委托出去的编辑/删除按钮在真实点击下仍作用于自己那个节点、**Profile 开关真的到达浏览器**（不解析 chrome://version 的本地化标签，而是看 Chrome 有没有把自有文件写进我们指定的目录；第二次启动不再往里种 Cookie，spy 只记 `_plant`；关闭后那个目录压根不被创建）、双击改名走对话框、草稿恢复保住节点 id 且后续新建不撞号、撤销重做保住 id 与用户起的名字、`escapeHtml` 在真 DOM 里把日志文本挡在元素之外（该层不写服务器：应用没有 data 目录覆盖开关，跑一次流程就会往真实 `data/` 与 `logs/` 里留东西） | 夹具层需 Chrome |
| `live_ollama`（marker） | 真调本地 Ollama 的活模型用例（服务不可达自动跳过） | Ollama |
| `live_site`（marker） | **全平台真实爬取 × 双浏览器模式**：知乎/微博/小红书/哔哩哔哩/YouTube 各自的搜索抓取、微信的推文正文抓取，都分别以 无头 与 可见窗口 两种模式真跑真断言；抖音因无头必被挡在验证码中间页，故以**可见窗口**真跑搜索＋逐视频详情，并单独断言「无头必须报可执行错误而不是交 0 行」；YouTube 反过来钉两种模式**结果一致**（它走页内 JSON），并真跑「某作者的作品」与「按频道链接再进一次」；X **只以可见窗口真跑**（搜索 22 行以上，证明虚拟列表确实被 id 窗口推着走；作者模式取 @NASA 时间线；无头那一路断言的是「绝不安静地交 0 行」——要么拒、要么给真数据），并真读一条**本次搜到的**推文的回复；哔哩哔哩 另真跑「某作者的作品」（UP 的 UID 当场从搜索行的 UP主ID 里取，
再走他自己的空间列表，断言每一行都属于这一个 UP、播放数仍是接口给的整数、游标里记着走的是谁的空间）、**热榜两张表真跑**（热门榜分页取到新的一批、周排行榜一次回整张表，并断言**请求数 ≤2 且不含任何 `web-interface/view`**——"这个模式不逐条取详情"是一句成本承诺，只能用计数证明；另断言两榜的集合确实不同、行字段与搜索模式完全一致）；抖音 也真跑「某作者的作品」（`sec_uid` 当场从**本次搜到并打开过**的视频页上取 `a[href*="/user/"]`，
再走他的作品网格：断言行数、每行链接与 视频ID 自洽、点赞数是真的整数、**行里不存在播放数列**、
每行都恰好一次视频页导航（一次主页导航 + 逐行详情，绝不为翻页重载主页），以及**每一行的作者名都等于主页自报的昵称**——
出现陌生作者名就说明网格或详情读串了人）；评论节点七平台真测（微博/小红书无头、哔哩哔哩无头游标翻页、知乎与抖音强制可见、YouTube 游标翻页、X 强制可见并按 id 窗口滚过第一屏；抖音以「评论数≥30 的视频至少收到 30 条」证明容器滚动真的在喂列表、(作者,内容) 不重复；YouTube 以「三个最热视频至少有一个翻页成功（>20 条）」证明游标没停在第一页）与**真浏览器 Cookie 中途失效→续跑**用例；知乎无头风控零结果按设计语义处理（明确无结果页→可执行报错，未触发请求的空壳→合法 0 行）；**知乎存下的正文按它自己那一页的长度来判**（同一个浏览器里再打开那条回答、读页面自身的正文长度，存下来的不得少于它的 70%——"搜索列表只给摘要"这件事，只有回答自己的页能判，拍一个字符常数只是猜）；无对应 Cookie 自动跳过 | Chrome + 已存 Cookie |
| `live_quick`（marker） | **`live_site` 的子集：每个可爬平台恰好一条真采集**（哔哩哔哩搜索、微博时间窗搜索、微信正文、小红书卡片、知乎搜索、抖音可见搜索、X 帖子形状、YouTube 无 facts 快路径、微博评论引擎），9 条、每条只取 3~5 行，是改动前后的日常闸门。`tests/unit/test_test_tiers.py` 钉住它必须是 `live_site` 的子集（否则"全量"会在没人注意时变小）、矩阵里每个平台都必须被某条 quick 用例真触及、总数不超过预算，并禁止整模块打这个标记（那样短层会长回长层）。`TestLiveCrawlerFixture` 另测**真机层脚手架本身**：一条用例向同一平台要第二个浏览器时，fixture 必须先关掉上一个并把这一轮交还——锁不可重入，否则会卡在 `crawl_gate` 里等 900 秒（没有浏览器弹出、没有输出，看起来只像"这条用例很慢"），有这条断言在，那种卡死当场就是红 | Chrome + 已存 Cookie |

```bash
# 快速套件（默认，约 70 秒，CI 友好；上面三层未打 marker 的部分）
python -m pytest -q

# 设备套件（真 Chrome + 真 Ollama）
python -m pytest -q -m "integration or live_ollama"

# 真站层按网络分两组跑：先国内（关 VPN），再海外（开 VPN）
python -m pytest -q -m "live_quick and live_cn"     # 国内：抖音/微博/知乎/小红书/B站/微信
python -m pytest -q -m "live_quick and live_os"     # 海外：X、YouTube
python -m pytest -q -m "live_site and live_cn"      # 全量同理，只在最终验收跑
python -m pytest -q -m "live_site and live_os"

# 覆盖率
python -m pytest -q --cov=backend --cov-report=term
```

修改代码后的完整验证流程见 `AGENTS.md` 的 "Change workflow"。
