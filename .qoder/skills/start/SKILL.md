---
name: start
description: Start the Flask dev server for the crawler workflow application. Use this when asked to run or start the app.
---

Start the Flask dev server (using the venv Python):

```bash
cd backend && ../.venv/Scripts/python.exe app.py
```

The server runs on http://localhost:5000. It may also be configured via the `PORT` environment variable.
