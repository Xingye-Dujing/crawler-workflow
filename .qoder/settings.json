{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json,subprocess,os; data=json.load(sys.stdin); f=(data.get('tool_response') or {}).get('filePath') or (data.get('tool_input') or {}).get('file_path',''); subprocess.run([os.path.join('.venv','Scripts','ruff.exe'),'format',f],capture_output=True) if f.endswith('.py') else None\" 2>/dev/null || true",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
